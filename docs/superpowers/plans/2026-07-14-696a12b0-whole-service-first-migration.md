# 696a12b0 Whole-Service-First Migration Plan

> 2026-07-15 10:29 EDT：A 的 follower-support `+1` 第十七领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十八窗，`claimBy=10:49:25`；B 第十七窗截止 `10:36:57`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 10:16 EDT：B 的 ObjectiveText recognize `+1` 第十六领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十七窗，`claimBy=10:36:57`；A 第十七窗截止 `10:29:20`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 10:09 EDT：A 的 follower-support `+1` 第十六领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十七窗，`claimBy=10:29:20`；B 第十六窗截止 `10:16:52`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 09:56 EDT：B 的 ObjectiveText recognize `+1` 第十五领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十六窗，`claimBy=10:16:52`；A 第十六窗截止 `10:09:14`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 09:49 EDT：A 的 follower-support `+1` 第十五领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十六窗，`claimBy=10:09:14`；B 第十五窗截止 `09:56:47`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 09:36 EDT：B 的 ObjectiveText recognize `+1` 第十四领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十五窗，`claimBy=09:56:47`；A 第十五窗截止 `09:49:09`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 09:29 EDT：A 的 follower-support `+1` 第十四领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十五窗，`claimBy=09:49:09`；B 第十四窗截止 `09:36:42`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 09:16 EDT：B 的 ObjectiveText recognize `+1` 第十三领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十四窗，`claimBy=09:36:42`；A 第十四窗截止 `09:29:04`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 09:09 EDT：A 的 follower-support `+1` 第十三领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十四窗，`claimBy=09:29:04`；B 第十三窗截止 `09:16:37`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 08:56 EDT：B 的 ObjectiveText recognize `+1` 第十二领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十三窗，`claimBy=09:16:37`；A 第十三窗截止 `09:08:29`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 08:48 EDT：A 的 follower-support `+1` 第十二领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十三窗，`claimBy=09:08:29`；B 第十二窗截止 `08:55:55`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 08:35 EDT：B 的 ObjectiveText recognize `+1` 第十一领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十二窗，`claimBy=08:55:55`；A 第十二窗截止 `08:47:52`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 08:27 EDT：A 的 follower-support `+1` 第十一领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十二窗，`claimBy=08:47:52`；B 第十一窗截止 `08:35:23`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 08:15 EDT：B 的 ObjectiveText recognize `+1` 第十领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十一窗，`claimBy=08:35:23`；A 第十一窗截止 `08:27:09`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 08:07 EDT：A 的 follower-support `+1` 第十领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十一窗，`claimBy=08:27:09`；B 第十窗截止 `08:14:43`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 07:54 EDT：B 的 ObjectiveText recognize `+1` 第九领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第十窗，`claimBy=08:14:43`；A 第十窗截止 `08:06:18`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 07:46 EDT：A 的 follower-support `+1` 第九领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第十窗，`claimBy=08:06:18`；B 第九窗截止 `07:54:02`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 07:34 EDT：B 的 ObjectiveText recognize `+1` 第八领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第九窗，`claimBy=07:54:02`；A 第九窗截止 `07:45:43`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 07:25 EDT：A 的 follower-support `+1` 第八领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第九窗，`claimBy=07:45:43`；B 第八窗截止 `07:33:35`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 07:13 EDT：B 的 ObjectiveText recognize `+1` 第七领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第八窗，`claimBy=07:33:35`；A 第八窗截止 `07:25:05`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 07:05 EDT：A 的 follower-support `+1` 第七领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第八窗，`claimBy=07:25:05`；B 第七窗截止 `07:12:40`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 06:52 EDT：B 的 ObjectiveText recognize `+1` 第六领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第七窗，`claimBy=07:12:40`；A 第七窗截止 `07:04:40`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 06:44 EDT：A 的 follower-support `+1` 第六领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第七窗，`claimBy=07:04:40`；B 第六窗截止 `06:52:10`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 06:32 EDT：B 的 ObjectiveText recognize `+1` 第五领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第六窗，`claimBy=06:52:10`；A 第六窗截止 `06:44:10`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 06:24 EDT：A 的 follower-support `+1` 第五领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第六窗，`claimBy=06:44:10`；B 第五窗截止 `06:31:35`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 06:11 EDT：B 的 ObjectiveText recognize `+1` 第四领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第五窗，`claimBy=06:31:35`；A 第五窗截止 `06:23:40`，
> C/D 已领取在途。hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 06:03 EDT：A 的 follower-support `+1` 第四领取窗到期仍无 true EOF `CLAIMED`，
> 已只向原 A 原样开启第五窗，`claimBy=06:23:40`；B 第四窗截止 `06:11:10`，C/D 已领取在途。
> hard ledger `189/407`、去重待构建池 `53`；writers 稳定前不跑 Maven。

> 2026-07-15 05:51 EDT：B 的 ObjectiveText recognize `+1` 第三领取窗到期仍无 true EOF
> `CLAIMED`，已只向原 B 原样开启第四窗，`claimBy=06:11:10`；不内部接管、不改 countUnit/写集。
> A 第四窗截止 `06:03:15`，C/D 已领取继续在途。hard ledger `189/407`、去重待构建池 `53`；
> writers 稳定前不跑 Maven。

> 2026-07-15 05:39 EDT：hard ledger `189/407`；I47 world-map route abandoned 由 runnable Navigation
> second-navigation 整链闭合并 source-approved，去重待构建池 `52 -> 53`。I46 identity sync 因 active
> `syncAll` caller 与 exact retained `GameContext.State/me` 投影均缺失，父级 `P1=2/countDelta=0`。I46/I47
> 槽已释放；H7 返回 `NONE`，当前无同时满足 runnable/matrix-exact/未重复/互斥的候选，不用 private helper、
> facade 或 non-compiling Task caller 凑数。A 第三窗未领后第四次原样重发，B 第三窗、C/D 在途；writers
> 稳定前不跑 Maven。

> 2026-07-15 05:34 EDT：hard ledger `189/407`；I43 TaskMaintenance start 与 I45R world-map failure
> source-approved，去重待构建池 `50 -> 52`。I44 因唯一 FAST caller 仍在 non-compiling whole Task 被父级
> `P1=1/countDelta=0`。Internal 转 I46 identity sync、I47 route abandoned；A/B 第二窗未领后分别第三次
> 原样重发，C/D 保持原 worker 在途。writers 稳定前不跑 Maven。

> 2026-07-15 05:18 EDT：hard ledger 仍 `189/407`，去重待构建池 `50`。I40 route-plan facts、I41
> fresh story blocker、I42 UI-clean lightweight 分别因后基线缺 caller/typed producer 与重复清理 owner 被父级
> `P1=2/2/1，countDelta=0`；三槽即时换发 I43 TaskMaintenance start initialize、I44 BattleRadar expected-exit
> consume、I45R world-map route failure。重复的 AutoPanel ensure-visible 单已立即中止（I7 已 source-approved）。
> C 与 Internal writers 稳定前不运行 Maven。

> 2026-07-15 04:13 EDT：ledger 仍 `189/407`；03:42 后新增 I22、A enter、D smart-click R1、I23、I25、
> I27 六个去重源码放行，`SOURCE APPROVED / COUNT PENDING BUILD` 为 `44`。External A/B/C/D 与
> Internal I30/I31/I32 七条 `countDelta=+1` 实现线均有任务；I24/I26/I28/I29 分别因重复边界、无 active
> caller 或缺 closed warning transport 保持 `countDelta=0`。writers 稳定前不运行 Maven。

> 2026-07-15 03:42 EDT：ledger 仍 `189/407`；smart-click proof 因 sourceTask 未校验撤销一项假通过，I18
> baseline lifecycle 与 I20 CommonBox role-clear 新增两项源码放行，净计 `38` 项待 unified build。External
> A/B/C/D 与 Internal I21/I22/I23 七条实现线已补满；C 继续持有 29-Java TaskTracker 完整整链，writer 稳定前
> 不运行 Maven。

> 2026-07-15 03:25 EDT：ledger 仍 `189/407`；去重后 37 个 count unit 已 `SOURCE APPROVED / COUNT PENDING BUILD`。
> C 的 TaskTracker 同一 `+1` 整单已按真实 emitter/retained/broker/final-consumed 缺口最终扩为 29 Java 文件，
> 直接实施且不再拆零计数前置；I16 suppression gate 因缺既有 producer/真实 caller 阻断并释放槽位。

> 2026-07-15 03:10 EDT：ledger 仍 `189/407`；父级新增批准 BattleRadar dynamic polling、Navigation current-map
> macro、AutoCombat exit recovery、AutoCombatPanel typed refresh、TaskMaintenance summon cache-state，去重后 32 项待
> unified build。I14 归队自检因既有 CR244 coordination owner 缺失 `BLOCKED P1=1/countDelta=0`；不在 Task 造 owner。
> C 已领取 21-Java TaskTracker 整链，writer 稳定前不运行 Maven。

> 2026-07-15 03:03 EDT：ledger `189/407`；新增 AutoCombat combat-maintenance source approval，去重后 27 项待
> unified build。TaskTracker whole read 的原六文件 scope 因双端 dormant 被父级 `BLOCKED P1=2`，但同一 countUnit
> 已原地扩为 21 Java 文件完整双仓实现，不拆零计数。A/B/C/D 与 I12/I13/I14 七条实现线全开，helper 提前排下一轮。

> 2026-07-15 02:47 EDT：ledger 仍 `189/407`；父级新增放行 follower first-aid 与 AutoCombatPanel record-exit，
> 去重后 26 个完整 count unit 等待 unified fresh build。BattleRadar stale-discard 与 TaskMaintenance private
> maybeClean 因非独立矩阵单元/同 bullet 重复而 `countDelta=0`。External A/B/C/D 当前分别为 BattleRadar dynamic
> polling、Navigation current-map macro 实施、TaskTracker whole read、AutoCombat combat-maintenance；内部完成旧单审查后
> 立即续派互斥独立 +1，不再发同一 caller chain 内的 helper 计数单。

> 2026-07-15 02:33 EDT：ledger 仍 `189/407`，已有 24 个互不重复完整 count unit 父级源码通过、等待
> unified fresh build。I9 private `detectBox` 与 C private `resolveTaskCode` 分别复用了已批准的 member-detect 与
> combat-maintenance 链，已按重复计数 `P1/countDelta=0` 剔除。External 当前 A BattleRadar stale-exit、B
> Navigation current-map active macro、C TaskTracker whole read、D AutoCombat follower first-aid；Internal I9 review
> closure、I10 TaskMaintenance summon-clean、I11 AutoCombatPanel record-exit。任务只接受真实新增整链，不再把同一链内
> helper/policy 分支重复计数。

> 2026-07-15 01:55 EDT：ledger 仍 `189/407`，当前 19 个完整 count unit 已父级源码通过、等待 writer
> 稳定后的统一 fresh Cloud package + DHXY compile。最新新增 MapName canonicalize、Dialog prepared-validation/
> detect-type、BattleRadar baseline-refresh、LeftTop follower-safe、CommonBox has-pending。External A 已交
> BattleRadar expected-exit consume 证据待父级复核，B/C/D 已收到 Dialog green-template、LeftTop supported-task、
> AutoCombat pending CommonBox 三张 `+1` 单；Internal I6/I7/I9 分别负责 cached-first-aid、panel ensure-visible、
> CommonBox detectBox。TaskMaintenance capability gate 因 active producer 缺失精确阻塞，不制造默认放行。

> 2026-07-15 00:58 EDT：ledger 仍 `189/407`，统一构建尚未运行。source-approved / pending-build 为
> I3 TaskMaintenance、I5 SummonSkill、A BattleRadar、I4 LeftTop、I1 CommonBox；CommonBox 的生产 Spring bean
> 图已由原 I1 精确 import 修复并获父级 Review #3。External A/C/D 已领取 AutoCombatPanel/AutoCombat/Navigation；B 的
> white-story 因真实 caller、handler owner、PNG/SHA 与 infra-terminal 四个 P1 被 park，并立即改发可达的
> `DialogService::handleDialog (+1)`。Internal 当前 I1 TeamReturn click、I4 PlayerFirstAid、I6 ClientIdentity 三槽全开；
> I2 TeamReturn 与 NpcClick 分别因 caller/时序和 shared lane 阻断，不计数、不造 stub。

> 2026-07-15 00:42 EDT：计数门进入七线实作。External A/B/C/D 已领取 BattleRadar/Dialog/PlayerState/Navigation
> `+1` 整链；Internal I2/I4/I5 分别实施 TeamReturn/LeftTop/SummonSkill。I3 TaskMaintenance 与 I1 CommonBox
> 已父级源码通过，等待所有 writer 稳定后的统一 DHXY compile + Cloud clean package；ledger 构建前保持
> `189/407`，构建通过后按批准顺序原子推进。

> **For agentic workers:** Execute whole-Service cohorts only. This repository is in no-local-test mode; do not create or run tests. Do not commit, reset, checkout, clean, delete, or start any runtime/input path.

**Goal:** First preserve the complete pre-cloud Service layer from DHXY commit `696a12b0ffb8aa21f7d5dee841a65cecd78be9f7` in the Cloud repository, then remove the user-selected local-only duplicate Services, repair the resulting Cloud-to-local dependency boundary, and only then extract physical actions from the remaining Cloud Services.

**Architecture:** `696a12b0` is the user-selected pre-cloud source baseline. Migration is deliberately sequential, not interleaved: (1) make an exact 32-file Service mirror in the Cloud repository; (2) promote complete classes as the active Cloud Service layer, then remove the four Services the user has explicitly retained locally; (3) use compile errors and complete call-graph comparison to add only the Cloud-to-local boundaries required by those removals; (4) replace desktop-only calls in the remaining Cloud Services at their original call sites with the existing typed `RemoteGameClientPort`, ordered `InputBundle`, or one closed local macro. Runner packages are not copied to Cloud.

**Tech Stack:** Java 17, Spring Boot, Lombok, Maven, existing Cloud `RemoteGameClientPort`, DHXY local command handler and single input queue.

## Latest Implementation Progress (2026-07-15 00:12 EDT)

- Whole-Service preservation remains complete: baseline `32/32 exact`; after removing the four user-selected
  local Services, active Cloud remains `28 exact / 4 expected-missing / 0 divergent`.
- A/C/D dedicated NPC contract cohorts were preflighted and independently reviewed by the parent. A is
  `BLOCKED P1=1` for narrowing tooltip null/blank/empty inputs before their closed terminals; C is
  `BLOCKED P1=1/P2=1` for a reachable throwing port stub and weakened PNG evidence invariants; D is
  `BLOCKED P1=2/P2=1` for half-registered sealed/kind dependencies, a Cloud-to-DHXY source dependency and
  weakened PNG evidence invariants. The original A/C/D workers all claimed their exact R1 repair sets before
  the 20-minute gate.
- B accepted the 19-Java white-story scope amendment and is actively writing the whole caller-to-handler chain.
  `STORY_MISS/STORY_ABSENT` may carry only the actual detection rect+dimensions needed to reproduce the
  `696a12b0` miss/absent actions; supplied frames use PNG bytes+SHA+rect+type with a pre-mechanics SHA gate.
- No Maven build runs while these four writers are active. Dedicated contracts still do not count as completed
  caller chains; approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 23:25 EDT)

- C delivered option-OCR R1. The supplied SHA request tree, three legal rect/frame forms and the main
  RAW/green/yellow caller routing are now structurally present, but parent review still blocks the chain
  (`P0=0/P1=3/P2=1`). The Cloud image port hashes optional null variants, local per-color wash exceptions still
  collapse the whole chain instead of selecting the baseline raw/green fallback, and the supplied SHA gate remains
  inside mechanics rather than before the handler invokes mechanics. Touched public contract comments also describe
  the obsolete all-present shape.
- The original C owns R2 in the same shared slot until `23:45:42`. Queue #26 has already prepared the complete
  B white-story, D player-anchor caller and A tooltip caller tasks in that strict order. No Maven build runs;
  approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 22:54 EDT)

- C delivered the complete two-repository same-frame option-OCR chain. After the non-binding helper precheck,
  the parent independently blocked it (`P0=0/P1=3/P2=0`): supplied frame bytes are stripped from request digest
  without a supplied SHA; the all-three-images-or-fail result drops the baseline raw OCR and yellow-wash failure
  fallbacks; and a detection rect is discarded whenever no reusable detection image is sent, changing the exact
  fresh-capture ROI to the default large dialog rect.
- The original C owns one R1 in the same shared slot, with the original 23 Java files plus its two previously
  approved OCR mechanics. It must add supplied-frame SHA binding, closed partial image availability with RAW pass,
  and rect-only exact fresh capture without moving target/alias/fallback decisions local. B white-story continues
  to wait; A/D source approvals remain frozen. No Maven build runs; approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 22:07 EDT)

- B prepared-action validation R2 is source-approved (`P0/P1/P2=0`): Cloud normalizes only null wash mode to
  `TEMPLATE_SPECIFIC`, both commands remain closed non-null, and the accepted mechanics/wire path is frozen. Its
  whole white-story chain waits for the single shared generic-wire slot.
- C option OCR words R1 is source-approved and C claimed the whole two-repository same-frame option-OCR chain at
  22:04. It owns the shared enum/codec/digest/handler slot until source release; green-first/yellow-fallback and
  Cloud business decisions remain unchanged.
- A tooltip R1 remains blocked (`P1=1`) because an empty match bypasses the post-capture binding recheck and can
  turn geometry drift into `NOT_FOUND`; the original A claimed its same-file R2 at 22:13:52.
- D player-anchor R1 closes all P1 interruption and normal OpenCV-owner findings plus the main typed invariants.
  Parent independently leaves only `P2=2`: native acquisition still begins before the try, and inclusive blob
  bounds accept the exclusive scanRect upper edge. The original D owns one same-file R2. No Maven build runs;
  approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 21:47 EDT)

- D's yellow-target R2 is source-approved (`P0/P1/P2=0`): the masked copy is flushed by the helper only on
  exceptional non-handoff and by the outer owner only after successful handoff. D immediately received the whole
  player-anchor local observation mechanics.
- B's R1 closes refresh/handle, 8/16 and all tri-layer measured-result invariants, but null wash mode still fails in
  the frozen DHXY codec/digest before reaching the baseline default wash. Parent corrected the repair route: Cloud
  normalizes null to TEMPLATE_SPECIFIC and both wire commands remain non-null; the original B owns a three-file R2.
- C's single OCR-provider call, unavailable/empty split, word order and image ownership are sound. Its same-file R1
  must close ImageIO/resolve runtime exits, null variant, strict PNG and long rect arithmetic (`P1=1/P2=2`). A's
  tooltip continuous mechanics remains in progress. No Maven build runs; approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 21:31 EDT)

- A prepared-point R1 is source-approved (`P0/P1/P2=0`): its public retry domain is exactly 0/1, its
  terminal/clickProduced invariants are closed, and the committed direct input-worker action chain is unchanged.
  A immediately received the whole task-tooltip continuous local macro.
- D's yellow-target R1 closes all four main repairs and preserves the exact yellow-shape algorithm, but remains
  blocked on one P2 owner leak when the masked-copy helper throws after allocating its local image. The original D
  owns a same-file R2.
- B's 20-file prepared-action validation chain, exact ImagePreprocessor add-backs, key/digest parity and normal 8/16
  path are preserved. It remains blocked (`P1=2/P2=2`) on refresh exception/handle closure, null wash-mode baseline
  parity, and closed command/result invariants; the original B owns a five-file R1. C's option OCR words observation
  is in progress. No Maven build runs while writers are active; approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 20:57 EDT)

- A R2 closed the requested strict frame/rect iff and validation-image finally owner. Parent's full public-entry
  rescan found one remaining P2: supplied ImageIO/runtime dimension failures occur before the raw-owner finally and
  can escape/leak. The original A owns one same-file R3; all accepted wash/evidence/capture behavior is frozen.
- C R2 inherited a same-frame authority amendment. `DialogDetection` does not structurally bind its raw path to its
  image, so both supplied and fallback branches must materialize the selected `frameImage` to the window-scoped raw
  artifact before wash/match. This adds no capture, retry, terminal or business decision.
- B's 20-file full Dialog chain and D's exact yellow-target mechanics remain active. No Maven build runs while
  writers are active; approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 20:49 EDT)

- Parent independently reviewed A/C R1 deliveries. Both preserve the main repaired 696 flow, but neither is
  source-approved: A still accepts rect-only supplied intent and silently performs a fresh capture, and its
  validation decode is not finally-owned (`P1=1/P2=1`). C's MATCHED evidence does not decode/re-hash/bind the
  PNG to rect/dimensions/relative-absolute point, while two collaborators may escape its closed entry
  (`P1=1/P2=1`).
- Narrow R2 repairs are issued to the original A/C with no write-set expansion. B's 20-file full Dialog chain
  and D's exact yellow-shape observation continue. No Maven build runs while writers are active; approved
  same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 20:43 EDT)

- D correctly stopped before fabricating a yellow shape-candidate producer. Parent resolved the prerequisite
  from Git history without a new design or user decision: the authoritative local pure-image closure is
  `696a12b0:GameTextLineOcrService.findYellowTextCandidateResult`, not current Cloud image processing and not
  the current DHXY yellow wash helper.
- D continues the same claimed single-file implementation. It must inline only the exact RGB-mask/shadow/
  connected-component/text-line/gap-split/score/sort closure as bottom nested implementation types, preserve
  the baseline default-window mask/skip branch on the same captured frame, preserve image-local to
  screen-absolute mapping, and return evidence for the exact prepared scan image that was scored. NPC OCR, target decisions, input,
  verification and fallback remain in Cloud. A/C repairs and B's 20-file full chain remain in progress;
  no Maven build runs while the four writers are active.

## Prior Implementation Progress (2026-07-14 20:36 EDT)

- Parent independently blocked A's first Dialog option OCR image preparation (`P1=1/P2=3`). The implementation
  used the template/highlighted-yellow wash for the baseline green-only first pass, so yellow options could be
  classified as green. Its public evidence also did not bind the three PNGs to one rect/dimension/hash authority,
  non-captured rect shape was contradictory, and refresh exceptions could escape the closed entry.
- The original A owns one same-class R1. It must call the exact green-only wash restored by B, keep yellow evidence
  mechanically available without changing the Cloud green-first consumption order, and close evidence/terminal
  invariants. B/C/D write sets remain disjoint; no build runs while writers are active.

## Prior Implementation Progress (2026-07-14 20:31 EDT)

- Phase 1/2 remain complete: `32/32 exact`, followed only by the four user-selected permanent-local removals;
  active remains `28 exact / 4 expected-missing / 0 divergent`, and approved same-path remains `189/407`.
- Parent independently blocked C's first white-story template observation (`P1=3/P2=2`). It removed the baseline
  one-shot fresh detection fallback, gated usable supplied frames on a new binding read, rejected a nullable
  template name after a valid hit, omitted the requested same-frame evidence, and released OpenCV Mats only on
  the success tail. The original C owns one same-class R1 using the existing `DialogDetectionLocalMechanics`;
  no design round or internal takeover is allowed.
- A continues the complete Dialog option OCR image preparation mechanics, B continues the 20-file Dialog
  prepared-action validation end-to-end chain, and D owns the complete NPC yellow-target observation mechanics.
  No Maven build runs while these Java writers remain active.

## Prior Implementation Progress (2026-07-14 15:59 EDT)

- Phase 1/2 remain complete and unchanged: exact Service evidence/promotion is `32/32`, then only the four
  user-selected permanent-local duplicates were removed. Approved same-path remains `189/407`.
- D's DHXY BattleRadar R1 is source-approved after restoring real OR/AND short-circuiting, separating capture
  exceptions from capture absence, and removing the permanent template cache. B's exact-window TaskTracker
  narrow/expanded/optional-drag/final-panel capture mechanics is also source-approved. C's first-aid flow preserves
  its two baseline paths, but parent independently confirmed a post-review P2: `HealOutcome` accepts one-sided
  click coordinates. The original C owns the compact-constructor-only R2 before resuming incense work.
- A's Npc Ctrl-probe mechanics is blocked with P1=2/P2=2: it has no caller-supplied exact binding and uses a Java
  callback as a purported closed intent, leaving the interleaved OCR/click/verify segment outside the macro. The
  original A owns the one-file repair; no internal takeover is allowed.
- Parent immediately dispatched the next disjoint implementations to released workers: B owns same-frame Dialog
  detection mechanics, C owns incense-status exact-window observation, and D owns story-dialog advance mechanics.
  A owns its Npc repair. A/B/C/D all have true-EOF claims at 15:52:05/15:52:16/15:58:30/15:56:30 EDT.
  C/D were authoritatively reissued at physical EOF after the first insertion landed in historical text. No build
  runs while these four writers are active.
- The advisory next-task helper has appended Candidate Queue #8 and closed. Delivery preflight appended its true-EOF
  closeout and closed; its one-sided-coordinate candidate was independently verified by parent before the C R2 was
  issued. Parent remains the sole reviewer and source-decision authority.

## Prior Implementation Progress (2026-07-14 15:40 EDT)

- Phase 1/2 remain complete and unchanged: exact Service evidence/promotion is `32/32`, then only the four
  user-selected permanent-local duplicates were removed. Approved same-path remains `189/407`.
- The BattleRadar Cloud contract (seven kinds, three closed fact families) is parent source-approved. A's R2
  `BattleRadarService` is also source-approved with seven stable slots and the complete 696 state/cadence graph;
  live caller integration remains pending because no task-entry holder `callWith` producer is wired yet.
- D's DHXY BattleRadar producer is bounded to one-file R1 after parent found three baseline deviations: lost
  OR/AND short-circuiting, capture exceptions reported as unavailable, and a permanent template cache. D claimed
  at 15:38:21. TeamReturn leader fact Repair #1 is source-approved after B made checkpoint exclusive to STOPPED.
- Four disjoint external tasks are now claimed: A owns the NpcClick Ctrl-probe closed local mechanics; B owns the
  TaskTracker exact-window narrow/expanded/drag/same-frame panel capture mechanics; C owns first-aid R1 restoring
  the separate no-focus probe and heal-all-direct paths; D owns the BattleRadar mechanics repair. No build runs
  while these writers are active.
- The fixed helper pipeline remains advisory only: delivery preflight is checking A/C materials and the next-task
  helper is preparing Queue #7. Parent remains the sole reviewer and dispatch authority.

## Prior Implementation Progress (2026-07-14 14:57 EDT)

- All four whole-Service tasks were claimed inside their original windows. B remains legitimately in progress on
  the complete `NpcClickService`; a claim is not a completion deadline and there is no internal takeover.
- Parent review blocked A's first `BattleRadarService` adaptation (`P1=3/P2=1`): it changed three baseline public
  signatures, had no reachable fact producer, prefetched later radar stages instead of preserving the original
  short-circuit timing, and used an invalid singleton/default-state context. The bounded R1 restores the 18-method
  responsibility graph and consumes seven closed facts at the original Stage 1/2/3/4 and avatar call sites.
- C and D correctly made zero Java changes to `PlayerStateService` and `DialogService`; both remain byte-exact to
  `696a12b0` and are prerequisite-blocked rather than falsely marked complete. Their released slots now implement
  the two disjoint halves of the BattleRadar producer: C owns the Cloud seven-kind/three-fact sealed contract and
  claimed it at 14:56; D owns the DHXY fact mirror, exact-window mechanical observation, and handler wiring.
- The delivery-preflight helper has recorded only non-binding evidence. The next-task helper is preparing Candidate
  Queue #5 while the parent independently reviews source and retains final `APPROVED/BLOCKED` authority. No build
  runs while these Java writers are active; approved same-path remains `189/407`.

## Prior Implementation Progress (2026-07-14 14:30 EDT)

- A LeftTop Repair #1、C CommonBox Repair #1 与 D TeamReturn member-button chain 均已父级独立
  `SOURCE APPROVED, P0/P1/P2=0`；B 的 TaskTracker rect 任务因缺 caller-reachable context 和同帧
  capture artifact 被正确 supersede，保持 baseline 原文件。
- 父级在四路 writer 稳定后执行 fresh Cloud `mvn -q clean package`。构建在 compile 阶段失败，失败面现只
  落在 7 个完整 Service：BattleRadar、ClientIdentity、Dialog、Navigation、NpcClick、PlayerState、
  TaskTrackerPanel。这个结果是下一批 Phase 4 的真实队列，不用零散 helper/DTO 数量粉饰进度。
- 其中 TaskTracker 先等显式 context caller 与 closed capture macro，ClientIdentity 的三级 title fallback
  等用户落点选择，Navigation X2 保留已登记 nested-exclusive P1。其余四个失败 Service 已在 `14:28 EDT`
  分别整类发给 External A/B/C/D：BattleRadar、NpcClick、PlayerState、Dialog；写集一人一文件互斥，
  统一领取截止 `14:48 EDT`，pre-cutover 版本仅作端口接法参考，行为只服从 `696a12b0`。

- Phase 1/2 remain complete: evidence mirror `32/32`, active promotion `32/32 exact`, then only the four
  user-selected permanent-local duplicates were removed; active comparison remains
  `28 exact / 4 expected-missing / 0 divergent` before boundary substitutions.
- The first complete permanent-local boundary, `UI_CLEAN`, is now source-approved end-to-end:
  External A caller substitutions, External B DHXY handler, External C Cloud contract/facade/wire, and
  External D DHXY DTO/codec/digest all have parent `P0/P1/P2=0`. Cross-side canonical parity is approved.
- Fresh DHXY `mvn -q -DskipTests compile` passes. Fresh Cloud `mvn -q clean package` still fails in compile,
  but no reported error belongs to `UI_CLEAN`; the failure consistently identifies the remaining whole-copy
  dependency closure: 20 desktop/window/capture/OCR/input collaborators, the deleted `BagService` and
  `GiveItemService` call-site types, plus Spring Boot/validation dependencies for `BotProperties`.
- Treat that failure as the Phase 3/4 work queue. Do not reopen approved UI_CLEAN code and do not copy desktop
  implementations merely to silence javac. Repair config/passive closure and permanent-local typed boundaries
  first, then replace desktop calls at their original baseline locations.
- The first follow-up closure is source-approved: Cloud POM now resolves the baseline Spring Boot validation/
  annotation classes, `CloudBagUseIncensePort` exposes the already-approved local macro, and the baseline
  `SheyaoxiangDigitTemplateReader` pure CPU helper is byte-exact in Cloud. A fresh Cloud package confirms the
  former `BotProperties` and digit-template missing-symbol errors are gone; the build still fails on desktop
  collaborators plus the remaining `BagService`/`GiveItemService` caller types, so overall package is not green.
- Parent integration re-review of the preflight evidence found one real Phase 4 gate. Five ordinary `UI_CLEAN`
  substitutions remain source-approved, but the three `NavigationService` X2 substitutions sit in baseline
  exclusive-input callbacks while the DHXY X2 handler acquires the remote exclusive queue again. Cloud's
  compatibility `InputSequences` intentionally cannot carry a cross-process `Supplier`. These three sites are
  therefore `INTEGRATION BLOCKED, P1=1` until each X2 + successful mouse-away + surrounding direct-input sequence
  is represented by one closed local macro. The shared `UI_CLEAN` wire and the other five callers are not reopened.
- The next-task helper queue is advisory. Parent accepted the LeftTop, CommonBox, and TaskTracker panel-rect
  candidates, rejected the claimed full AutoCombatPanel and full TeamReturn readiness because current facts do
  not cover their complete baseline mechanics, and requested a narrower honest replacement before issuing D.
- Candidate Queue Repair #1 is parent-approved for scheduling and has been issued at the physical EOF of all
  four External logs: A adapts the complete LeftTopStatusSwitch boundary; B adapts only TaskTracker panel-rect
  localization; C adapts the complete CommonBox observation/input boundary; D adapts only the TeamReturn member
  button method. The four Cloud Service files had exact baseline blobs immediately before dispatch. Claim
  deadlines are 13:55 EDT for A/B/C and 13:56 EDT for D. D's first insertion hit an old duplicate log anchor and
  is explicitly void; only the true-EOF reissue starts its claim window.
- Claim monitoring after dispatch: A claimed at 13:36:03, C at 13:36:55, D at 13:37:13 after the true-EOF
  reissue, and B at 13:40:00. All four claims were inside their deadlines and establish only write-set ownership;
  implementation may legitimately take longer than twenty minutes. At 13:41:36 no new Implementation/Repair had
  reached any of the four physical EOFs, so no build or review judgment was started prematurely.
- At 13:44:04 B correctly reported that the TaskTracker panel-rect brief had no consumable fact instance inside
  its one-file write set. Parent independently confirmed that the exact baseline Service receives only `source`,
  the current DecisionEngine request path has no retained task context, and the existing fact supplies
  `WINDOW_CLIENT_PX` geometry while the baseline method returns a screen-absolute captured artifact. The task is
  superseded as a parent prerequisite error (`P1=1/P2=1`), not a B implementation defect; the Service remains
  byte-exact until a caller-reachable typed panel artifact exists, and B will receive a disjoint replacement task.
- A's first LeftTop adaptation was parent-blocked for collapsing STOPPED/UNKNOWN/interrupt into ordinary failure and
  for logging a fabricated rectangle. Original A claimed and delivered the bounded repair. Parent independently
  verified explicit fact/input terminal matrices, interrupt unwind, honest `rect=null`, and the unchanged
  `MOVE_MOUSE -> SLEEP(120ms) -> CLICK_LEFT(250ms)` bundle; A Repair #1 is `SOURCE APPROVED, P0/P1/P2=0`, build pending.
- D's TeamReturn member-button method is `SOURCE APPROVED, P0/P1/P2=0`, build pending. Parent verified two fresh
  observation slots around the unchanged incense call, use of the second screen-absolute point, independent X/Y
  `[-3,3]` randomization, exact `CLICK_LEFT(150ms) -> SLEEP(500ms)` order, terminal unwind, timestamps, and logs.
- C's CommonBox first implementation is `BLOCKED, P1=3/P2=1`. The bounded repair must preserve STOPPED/UNKNOWN/
  interrupt as control or fatal outcomes, anchor the existing 30-second pending window at DHXY's true
  `matchedAtEpochMs`, and inline the newly split `recordMatched`, `consumeClick`, and `actionSlot` wrappers back into
  the baseline call sites. The original C owns the repair; the 20-minute gate checks claim only and never triggers
  internal takeover.
- The queue helper's first B replacement was withdrawn before dispatch after parent found that Cloud
  `TaskExecutionContextHolder.current()` has no real producer. Replacing the local fallback would silently collapse
  legitimate null-context calls to default-window/epoch-zero state. No Java was changed. The helper is now preparing
  a genuine two-sided TaskTracker cohort: DHXY owns exact-window localization, necessary drag, and same-frame capture;
  Cloud retains panel geometry, green-chain segmentation, fingerprint/cache, ordering, classification, and result
  construction. B/D will receive those disjoint implementation briefs only after the producer/caller chain is exact.

## Parent Review Pipeline (2026-07-14 13:15 EDT)

- The parent thread remains the only manager and the only source of final `APPROVED/BLOCKED` judgments.
- Internal helper slot 1 is a non-binding delivery preflight: it reads each new External A/B/C/D delivery first,
  checks claimed versus actual write set, highlights candidate P0/P1/P2 evidence, and gives the parent a compact
  review checklist. It never edits worker code or issues approval.
- Internal helper slot 2 maintains the next-task queue: before the parent finishes the current review, it prepares
  four disjoint, directly implementable A/B/C/D briefs plus backups, with exact write sets, prerequisites, and
  acceptance gates. Ambiguous local-versus-Cloud placement is marked `NEEDS_USER_DECISION`, never inferred.
- The parent independently verifies source and build evidence after preflight. Once a delivery passes, the prepared
  next brief can be posted immediately, avoiding an idle planning gap. These helper roles do not increase the
  two-internal-worker concurrency limit and do not count as reviewers.
- A helper may label a candidate `READY`, but that is not authority. Parent must reject or narrow any candidate
  whose existing typed fact cannot cover the exact baseline call site; planning speed never permits inventing
  missing observation fields or silently shortening a Service.

## Global Constraints

- Authoritative source tree: DHXY `696a12b0ffb8aa21f7d5dee841a65cecd78be9f7`.
- First actual cloud-code commit is `9aa987d1`; do not use `9aa987d1` or later Service bodies as business authority.
- `0114604e` is only the current migration workspace/transport starting point.
- Do not copy `runner/**` implementations into Cloud and do not create a Cloud Runner replacement.
- Phase 1 is exact source preservation only. It must not adapt imports, split actions, redesign methods, or attempt to compile the mirrored files.
- A Service is accepted only after the later active-tree comparison against `git show 696a12b0:<path>` proves every public/private method and behavioral branch is represented.
- Method/helper/DTO-only work is reusable material, not Service completion.
- Do not shorten logic to obtain compilation. During Phase 3, a missing dependency caused by deleting a local-only Service is repaired at the same call site by one typed local operation; surrounding control flow remains unchanged.
- Do not add owner/permit/session/ledger/compaction/durable workflow/business TTL/automatic retry.
- Preserve all dirty/untracked work. Workers have disjoint Service-file ownership and must not edit another cohort.
- No local automated tests. After writers stabilize, parent runs Cloud `mvn -q clean package`; DHXY Java changes additionally require `mvn -q -DskipTests compile`.

## Baseline Inventory

- `696a12b0` contains 32 files under `src/main/java/com/bot/dhxy/service/**`.
- Cloud currently has 24 same-path baseline filenames, but their existence does not prove baseline completeness.
- Eight baseline filenames are not currently present in Cloud: `BagService`, `ClientIdentityService`, `DialogService`, `GiveItemService`, `QuestManagerService`, `SystemPowerService`, `UICleanerService`, and `WorldMapRouteResultMemoryService`.
- Exact Cloud-repository mirror: `D:\mavenProject\dhxy-cloud-brain\migration-baseline\696a12b0\src\main\java\com\bot\dhxy\service\**`.
- Parent verification on 2026-07-14: `32/32` files, `1,110,791` total bytes, Git blob equality `BAD=0`, Runner files `0`.
- The eight previously missing active-tree files were copied with create-new semantics and parent-verified `8/8`, `BAD=0`; no existing file was overwritten.
- The 16 prior divergent active bodies are byte-preserved under `D:\mavenProject\dhxy-cloud-brain\migration-preserved\pre-696a12b0-whole-service-cutover-20260714T1129\**` with snapshot `BAD=0` before coordinated promotion.
- Two internal promotion cohorts and External A/B/C/D completed preservation-gated mechanical promotions. Parent review found `P0/P1/P2=0` for every cohort and the final active audit was `TOTAL=32 EXACT=32 MISSING=0 DIFF=0`.
- User-selected permanent local Services: `BagService`, `UICleanerService`, `GiveItemService`, and `QuestManagerService`.
- The exact mirror keeps all 32 files as immutable evidence. The four local-only Services have now been removed only from the active Cloud source tree, never from the evidence mirror and never from DHXY. Current active comparison is `28 exact / 4 expected-missing / 0 divergent`.
- The first post-removal Cloud `mvn -q clean package` failed at compilation as expected. The errors expose missing passive shared models/config, callers of the four removed local Services, and desktop-only/window/capture/OCR/input collaborators that remain for Phase 4. Phase 3 must reduce these in that order without shortening Service logic.

## Authoritative Four-Phase Sequence

### Phase 1: Complete Exact Mirror

1. Copy all 32 `696a12b0:src/main/java/com/bot/dhxy/service/**` files byte-for-byte into the Cloud repository mirror.
2. Verify every target with Git blob identity and SHA-256; do not change package/imports or chase compilation.
3. Stop all workers from adapting local mechanics in active Cloud Service files during this phase. Preserve their existing partial work without rollback.

### Phase 2: Active Whole-Service Cutover And Local Duplicate Removal

1. After every active Service writer has acknowledged the freeze, promote complete baseline classes to the active Cloud Service tree as coordinated, disjoint cohorts.
2. Confirm the active Cloud tree contains the complete Service graph before any local-only removal.
3. Remove only `BagService`, `UICleanerService`, `GiveItemService`, and `QuestManagerService` from the active Cloud tree. Their DHXY implementations remain unchanged and authoritative.
4. Do not remove any additional ambiguous Service without first asking the user to choose local or Cloud placement.

### Phase 3: Compile-Driven Boundary Repair

1. Run the Cloud compile/package gate after the four removals.
2. Classify each compile failure as a missing shared model, a missing local-only Service call, or an unrelated pre-existing/in-flight error.
3. Copy passive shared models unchanged where appropriate. Replace calls to the four local-only Services with the smallest typed local operation at the original call site.
4. Preserve all baseline branches, order, delays, fallback, state writes, and return values. Compilation is a dependency detector, not permission to redesign behavior.

### Phase 4: Remaining Desktop-Action Extraction

1. Scan the now-complete active Cloud Service layer for HWND, capture, template/OCR, watcher, mouse, and keyboard calls.
2. Ordinary physical sequences become one ordered `InputBundle` per original logical sequence.
3. Capture/template/OCR interleaved with input remains one closed local macro.
4. Review every replacement against the exact baseline call site, then run fresh Cloud package and applicable DHXY compile gates.

## Superseded Wave 1 Notes

The six assignments below were issued before the user fixed the sequential order. Their partial active-tree code is preserved, but all six are halted from further local-boundary adaptation until Phase 2. They do not count as complete Services and must not overwrite the exact mirror.

### External A: TaskTrackerPanelService

**Owned Java file:** Cloud `src/main/java/com/bot/dhxy/service/TaskTrackerPanelService.java`.

- Start from the complete `696a12b0` class, not the current eleven-method patch.
- Preserve every baseline public/private method, cache/fingerprint path, panel/detail geometry, ordering, delay, classification, and result construction.
- Replace exact-window capture/template/OCR/input only at their original call sites with typed remote operations.
- Deliver a baseline method inventory, explicit local-boundary substitution table, and non-clean Cloud compile evidence.

### External B: TaskMaintenanceService

**Owned Java file:** Cloud `src/main/java/com/bot/dhxy/service/TaskMaintenanceService.java`.

- Start from the complete `696a12b0` class, not the current 29/30 API patch.
- Preserve full maintenance coordination, queue, cooldown, team/session, combat-phase, fallback, and timing logic.
- Replace each local capture/dialog/input operation at its original position with a typed remote operation or closed macro.
- Deliver a baseline method inventory, substitution table, and non-clean Cloud compile evidence.

### External C: NpcClickService

**Owned Java file:** Cloud `src/main/java/com/bot/dhxy/service/NpcClickService.java`.

- Start from the complete 3,120-line `696a12b0` class.
- Preserve candidate order, screenshots, click verification order, tooltip/OCR/template fallbacks, delays, direct-combat branch, and all private helpers.
- Convert local capture/template/OCR/input calls at their existing sites; do not redesign the candidate algorithm or split it into a new workflow.
- Deliver a baseline method inventory, substitution table, and non-clean Cloud compile evidence.

### External D: NavigationService

**Owned Java file:** Cloud `src/main/java/com/bot/dhxy/service/NavigationService.java`.

- Start from the complete 2,750-line `696a12b0` class, superseding the single `navigateInCurrentMap` adapter as the completion unit.
- Preserve route selection, map-memory behavior, dialogs, movement checks, 60-second loop, keep-turn, cleanup, status mapping, timing, and all private helpers.
- Local pathing/capture/template/OCR/input remains DHXY mechanics behind typed operations; do not copy Runner or create a second navigation state machine.
- Deliver a baseline method inventory, substitution table, and non-clean Cloud compile evidence.

### Internal CG: PlayerStateService

**Owned Java file:** Cloud `src/main/java/com/bot/dhxy/service/PlayerStateService.java`.

- Rebase the entire class on `696a12b0`; discard the method-slice completion model.
- Preserve per-target capture followed immediately by input, first-aid/supply/incense order, cache behavior, delays, and every private helper.
- Replace local mechanics at existing sites with typed operations without batching or reordering.

### Internal CH: AutoCombatService

**Owned Java file:** Cloud `src/main/java/com/bot/dhxy/service/AutoCombatService.java`.

- Rebase the entire class on `696a12b0`; the previously retained 12 methods are only reusable blocks.
- Preserve initialization, tick orchestration, BattleRadar interactions, first-aid queue, deferred recovery, deadlines, cleanup, and all private helpers.
- Consume typed local battle facts and UI-clean operations at the original call sites; do not add a new host, thread, or retry model.

## Review And Gate

For each Service, parent review must produce:

1. Baseline file SHA and current Cloud file SHA.
2. Baseline public/private method list and one-to-one disposition.
3. Branch/order/delay/fallback/state comparison.
4. Exact list of replaced local call sites and the typed operation used.
5. `APPROVED` or `BLOCKED` with P0/P1/P2 evidence.

No same-path count changes during Wave 1 merely because files compile. Count changes only after the complete Service and its reachable caller/local-terminal chain pass parent review and fresh builds.

## Phase 3 Progress - 2026-07-14 12:37 EDT

- The 32-Service evidence mirror remains `32/32`, `1,110,791` bytes, `BAD=0`, Runner `0`. Active Cloud is
  `28 exact / 4 expected-missing / 0 divergent` after deleting only the four user-approved local Services.
- The parent approved the repaired `ReturnItemPrescanService` boundary with `P0/P1/P2=0`. Its seven baseline
  public methods remain present, the non-baseline `hasCached` API is absent, and each approved
  `BAG_RETURN_ITEM` operation occurs exactly once.
- The `UI_CLEAN` boundary is split across four disjoint external write sets. A/B/C/D claimed at `12:33:59`,
  `12:36:26`, `12:37:24`, and `12:35:27` respectively. A waits for C's Cloud facade; B waits for D's DHXY
  payload types; these are declared compile dependencies after valid claims, not idle ownership.
- No Maven build runs while those Java writers are active. The approved same-path count remains `189/407`
  until parent source review and both applicable fresh build gates pass.

## Phase 3/4 Progress - 2026-07-14 16:31 EDT

- Completion accounting now uses coherent reachable chains, not delivery speed or file count. Phase 1/2 remains
  complete: the exact 32-Service graph was preserved first, then only the four user-selected local duplicates were
  removed. Active Cloud remains `28 exact / 4 expected-missing / 0 divergent`; approved same-path remains `189/407`.
- External A's first Ctrl-probe local mechanics is parent-blocked (`P1=5/P2=2`) until local OCR/Dialog/Battle
  prerequisites exist. Rather than leave A idle, the parent issued a disjoint three-file whole-lifecycle cohort:
  full `696a12b0` `AutoBattleTask` plus explicit `TaskExecutionContextHolder` coverage in `BaseTaskTemplate` and
  `TaskStepExecutor`, closing public task execution through the existing BattleRadar typed terminal.
- External C's FirstAid mechanics repair is source-approved and C claimed a four-file local OCR-to-incense cohort.
  External B is repairing the Dialog detection terminal/resource invariants (`P1=1/P2=4`); External D claimed the
  exact-binding gate repair for StoryAdvance (`P1=1`). The original workers retain every repair; no internal takeover.
- The queue helper must now prepare only 2-5 file coherent caller-to-terminal cohorts or complete large classes. It
  may not count DTO-only, one-line, or private-helper deliveries as Service progress. Parent source review and fresh
  Cloud/DHXY build gates remain mandatory after all writers stabilize.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Whole Task Preservation - 2026-07-15 05:10 EDT

- Active Cloud now contains byte-exact baseline copies of `WubeiTask`, `FiveRingTaskV2`, and `XiuluoTaskV2`.
  Their blobs match `696a12b0`; no workflow method, delay, fallback, state or comment was removed.
- These files are evidence preservation, not counted completion. Their execute units remain blocked on the single
  shared task-runtime/turn/event owners and closed DHXY Bag/UI-clean/QuestManager/capture/OCR/input mechanics.
- I40/I41/I42 continue with three independent active typed chains while C owns the shared TaskTracker writer lane.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Task Caller Recovery - 2026-07-15 05:03 EDT

- Two attempted phase/helper count units were correctly rejected because their Cloud production callers do not
  exist. They remain `countDelta=0`; no enum/private helper self-consistency is counted.
- Internal I37/I38/I39 now promote the complete baseline `WubeiTask`, `FiveRingTaskV2`, and `XiuluoTaskV2`
  entry classes as three disjoint whole-task units. This restores the missing task/phase consumers before the
  parked phase rows are reconsidered.
- Hard ledger remains `189/407`; the deduplicated source-approved build pool remains `50` until fresh Maven.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Count Ledger Checkpoint - 2026-07-15 04:40 EDT

- Hard ledger remains `189/407` until the same-round fresh Maven gate; the deduplicated parent source-approved pool
  is now `50` (`44 -> 50`) after A polling, I30/I31/I32, I33 idle maintenance and D initialization passed with
  `P0/P1/P2=0`.
- B incense status remains `BLOCKED_SHARED_LANE`, `P1=1/countDelta=0`: Cloud still performs mouse-away locally
  before remote capture. Preserve the implementation and close the same unit through the existing DHXY incense
  observation mechanics after C's shared protocol lane stabilizes.
- Seven implementation lanes are occupied: External A follower-support, B Wubei terminal, C TaskTracker whole
  READ/MATERIALIZE, D Xiuluo terminal; Internal I34 summon business-dialog, I35 FiveRing terminal, I36 Wubei
  context-next. No build runs while Java writers are active.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Count Progress - 2026-07-15 01:30 EDT

- Parent source-approved two additional real count units: BattleRadar fast expected-exit and LeftTop combat
  maintenance. Together with the prior eleven, thirteen units are pending the unified fresh Maven gate; the ledger
  intentionally remains `189/407` until that gate succeeds.
- Parent source review rejected Navigation `navigateInCurrentMap` as a false NO_CODE_CHANGE closure: the active Cloud
  method does not invoke the registered local macro and still runs window/runtime/mini-map mechanics in Cloud.
  It is `BLOCKED P1=1` and contributes no count.
- The seven mutually exclusive implementation lanes now target BattleRadar refresh, AutoCombat refresh, LeftTop
  follower safe-window, Dialog prepared validation, MapName canonicalization, NpcClick smart, and PlayerState position.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Count Gate Progress - 2026-07-15 01:19 EDT

- Ledger remains `189/407` until the unified fresh build. Four additional complete count units are now parent
  source-approved and pending that build: Navigation `navigateToNPC`, PlayerState startup first-aid, TeamReturn member
  click, and ClientIdentity sync. Together with the five earlier approvals, nine units are build-eligible in order.
- The seven implementation lanes remain occupied by whole count units: External A panel alignment, B dialog handle,
  C BattleRadar fast exit, D current-map navigation; Internal I1 CommonBox member detection, I4 left-top combat
  maintenance, and I6 binding-title resolution. No DTO/helper-only task is counted.
- AutoCombatPanel whole verify remains blocked only on the missing rounds shared fact; AutoCombat whole tick remains
  blocked on panel and incense typed terminals. Those blockers are parked without consuming A/C and without internal
  takeover. Maven stays deferred while Java writers are active.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Countable-task gate - 2026-07-15 00:24 EDT

The user replaced the prerequisite/cohort completion model with a strict progress-unit model.

1. Every newly issued implementation task must name exactly one existing migration-ledger `countUnit` and
   `countDelta=+1`.
2. The task must include every prerequisite needed to make that unit reachable from its real public caller through
   the Cloud Service and typed DHXY primitive/mechanics to a closed terminal/result. DTOs, enums, ports, codecs,
   handlers, helpers, mechanics, source-only approval, or compilation alone are not independently complete tasks.
3. Parent source approval and the applicable fresh Maven gates are part of the same completion definition. In the
   acceptance round the ledger moves atomically from its then-current value to `before + 1`; a task cannot be marked
   complete while the ledger stays unchanged.
4. A task that discovers a required write outside its exclusive reservation reports `BLOCKED`; it must not split the
   missing prerequisite into a new zero-count filler task or install an unreachable stub.
5. The implementation pool is four user-owned External Workers plus three Internal implementation Workers. Review
   and queue helpers may run separately, but they do not own Java implementation count units and do not replace the
   parent's final review.
6. The legacy A/C/D contract repairs issued before this gate are closed in place. After release, every follow-up uses
   this countable-task gate.

Current seven implementation lines are: BattleRadar, Dialog white-story, PlayerState after C's final strict-PNG
repair, Navigation, CommonBox, TeamReturn, and TaskMaintenance. No approved business difference; migrate
behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 23:47 EDT

- C option-OCR R2 is parent source-approved (`P0/P1/P2=0`) after optional-SHA, per-color wash exception,
  pre-mechanics supplied-SHA and public-contract repairs. The shared write set is released.
- The parent found an orchestration failure: A, B and D had no next implementation task for roughly one hour because
  the queue serialized whole chains on the generic LocalMacro files. The queue now serializes only shared integration.
- B owns the complete white-story caller-to-terminal chain and the shared slot. In parallel, A owns the ten dedicated
  tooltip/prepared-point contracts, C owns the five yellow-target contracts, and D owns the five player-anchor contracts.
  These three cohorts are mutually exclusive and do not touch the shared twelve files or `NpcClickService`.
- All four external tasks are truly claimed at EOF: A `23:49:09`, B `23:49:40`, C `23:50:00`, and D `23:52:00` EDT.
- The dedicated cohorts do not count as complete paths. After B releases the shared slot, one integration wave closes
  enum/permit/request/outcome/codec/digest/handler and the existing Cloud callers. Builds remain deferred until writers
  stabilize. Active remains `28 exact / 4 expected-missing / 0 divergent`; approved same-path remains `189/407`.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 22:26 EDT

- A's task-tooltip local macro Repair #2 is parent source-approved (`P0/P1/P2=0`): every template capture now
  revalidates the same normalized HWND and unchanged valid geometry before either the empty or nonempty branch;
  the nonempty branch uses that same post-capture binding origin for the learned ROI. Input-worker ownership,
  0.82/36, physical sequence, verify count, Y+90/ROI and the seven terminals remain unchanged.
- D's player-anchor local observation Repair #2 is parent source-approved (`P0/P1/P2=0`): all six OpenCV owners
  are null-first and acquired inside one try with exact-once nonnull release, and inclusive blob bounds are now
  strictly below the exclusive scan-rect upper bounds. The baseline interruption fences, capture, mask, HSV/blob,
  evidence and Cloud/local decision boundary remain unchanged.
- A and D have source-approved complete local mechanics but not reachable caller chains, so neither advances
  `189/407`. The reviewed shared-write order remains `C option-OCR -> B white-story -> D player-anchor caller ->
  A tooltip caller`; no filler leaf is issued while C owns the generic wire and DialogService write set.
- C's complete same-frame option-OCR two-repository chain remains in flight; B waits for its released shared slot.
  No concurrent Maven build is run while that writer is active.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 21:19 EDT

- Active Cloud remains `28 exact / 4 expected-missing / 0 divergent`; approved same-path remains `189/407`.
- C's white-story R3 is parent source-approved (`P0/P1/P2=0`): every selected supplied/fallback frame is
  materialized to a new window-scoped raw artifact, and evidence, wash and template matching consume that same
  authoritative frame. C has claimed the next whole option-OCR word observation mechanic.
- A's prepared-point direct input-worker sequence preserves move/150/click-hold150/wait/verify, the optional
  retry wait and zero queue nesting. It is parent-blocked (`P1=1/P2=1`) because the public intent permits two or
  more retries although all four baseline callers use only 0/1, and the public result does not enforce its
  terminal/click-produced combinations. The original A owns one bounded same-file repair.
- B's 20-Java Dialog validation caller-to-terminal chain and D's yellow-target R1 are delivered. The preflight
  helper is collecting non-binding evidence; the parent will independently review both before any build.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 21:06 EDT

- A Dialog option OCR image R3 is parent source-approved (`P0/P1/P2=0`) after closing supplied decode/dimension
  runtime exceptions and exact image ownership. A immediately received the complete prepared-point click/verify local
  mechanics; the next shared wire/caller step remains deferred until B releases the shared family.
- C white-story R2 is parent-blocked (`P1=1/P2=1`): it still reuses a supplied raw path, so returned evidence and
  template-match pixels may come from different frames. C R3 must always materialize the selected frame to a
  window-scoped artifact and close frame/save runtime exceptions.
- D yellow-target I1 is parent-blocked (`P1=3/P2=1`): it omitted the default-mask/skip policy, mapped refresh failures
  to the capture terminal, added a post-capture interruption checkpoint, and left public result invariants weak. The
  exact yellow predicate/component/shadow/score algorithm is frozen while D repairs the same file.
- B's 20-Java Dialog prepared-action validation cross-repo chain remains in implementation. All four current write
  sets are disjoint; no Maven build runs until writers stabilize. Approved same-path remains `189/407`.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 20:22 EDT

- Active Service audit remains `28 exact / 4 expected-missing / 0 divergent`; approved same-path remains
  `189/407` until a complete reachable chain and fresh build both pass.
- A's complete AutoCombatPanel rounds observation mechanics and D's Dialog green-template R3 are parent source-
  approved (`P0/P1/P2=0`). A and D immediately received the full Dialog option OCR image-preparation and NPC
  yellow-target observation mechanics respectively, with disjoint write sets.
- B claimed the complete Dialog prepared-action validation chain. Its blocker correctly identified four missing
  baseline image/fingerprint methods on the DHXY side. The parent amended the same task to add DHXY
  `ImagePreprocessor.java`, permitting only the exact four-method `696a12b0` add-back. The cohort is now 20 Java
  files and still must deliver the whole Cloud caller -> Service -> transport -> DHXY mechanics -> terminal chain.
- C's full white-story template observation mechanics remains claimed and in progress. No Maven build runs while
  B/C/D or helper queues are writing.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 20:14 EDT

- A's repaired AutoCombatPanel visibility/alignment mechanics and C's single-X PlayerState result invariant are now
  parent source-approved (`P0/P1/P2=0`). Together with B's approved DHXY half, the PlayerState first-aid cross-side
  source acceptance domain is aligned; fresh builds still gate completion and `189/407` remains unchanged.
- D closed the nullable/invalid candidate-order P1. It remains blocked on two bounded P2 invariants only: supplied
  PNG dimensions must equal the supplied screen rect dimensions, and the decoded ImageEvidence validation image
  must be flushed exactly once.
- The next wave is already issued, not merely queued: B owns a 19-Java complete Dialog prepared-action validation
  caller-to-terminal chain; A and C own substantial whole continuous rounds and white-story mechanics; D owns the
  two-point same-file repair. This satisfies the rule that at least one task in every wave is a complete cross-side
  reachable chain rather than four prerequisite-only leaves.
- No Maven build runs until these mutually exclusive writers stabilize.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 20:10 EDT

- Phase 1/2 remains complete and unchanged: active Cloud was first promoted to 32/32 exact, then only the four
  user-selected local Services were removed. The active audit remains `28 exact / 4 expected-missing / 0 divergent`;
  approved same-path remains `189/407`.
- B's DHXY half of the PlayerState first-aid chain is parent source-approved after restoring the baseline
  pre-capture `baseX==-1` unavailable gate and the mirrored closed-result invariant. C's Cloud R2 is still blocked
  because it widened that single X sentinel to X/Y; C R3 may only remove the extra Y restriction.
- A delivered a 388-line whole continuous AutoCombatPanel visibility/alignment mechanic. It is parent-blocked on
  two exact baseline sequences: first capture-unavailable must still take the single Alt+8 fallback used after a
  baseline null, and a drag must still perform exactly one post-drag observation even when the settle sleep returns
  false. D's R2 must preserve null/invalid template candidates as caller-order skips rather than aborting the pass.
- Delivery preflight remains non-binding. Queue planning now has a hard throughput rule: each next four-worker wave
  must include at least one complete cross-side reachable caller/service-to-local-terminal chain; the remaining
  disjoint slots may carry substantial whole mechanics. Four prerequisite-only tasks are not an acceptable wave.
- No concurrent Maven build runs while A/C/D writers are active. Source approval, cross-side parity and the fresh
  Cloud/DHXY build gates remain required before the count changes.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 19:38 EDT

- Active Cloud remains `28 exact / 4 expected-missing / 0 divergent`; approved same-path remains `189/407`.
  The current A/B/C/D deliveries are substantial mechanics or 9+10-file active chains, but delivery speed and
  worker self-QA do not satisfy the parent gate.
- A's three-operation story-objective mechanics is parent-blocked (`P1=3/P2=1`) for fresh/stale geometry mixing,
  loss of the 696 detection-frame crop fallback, new entry interruption gates, and image-result integrity. A claimed
  the same-file R1 at `19:34:59`.
- B's nine-file DHXY PlayerState first-aid half is parent-blocked (`P1=1/P2=2`) for queue-time stale HEAL geometry
  and cross-repo constructor acceptance-set mismatch. B claimed R1 at `19:34:40`. C review additionally proved the
  PROBE plan base must be returned from the same capture-time geometry; B received a bounded codec/digest amendment.
- C's ten-file Cloud PlayerState half is parent-blocked (`P1=1/P2=1`) until the separate pre-probe GEOMETRY fact is
  removed, the typed PROBE result carries `observedBaseX/Y`, and both sides enforce the fixed four-bar identity/order.
- D's green-template mechanics is parent-blocked (`P1=4/P2=2`): the prepare supplied-frame path is missing, the
  click path incorrectly reuses a detection frame that 696 recaptures, an unreadable template aborts later candidates,
  capture geometry can be stale, and best-match/image-evidence diagnostics are incomplete. The parent explicitly
  corrected the task brief where it conflicted with the click baseline.
- No build runs while these Java writers repair the contracts/mechanics. The delivery helper is preflighting C/D;
  the queue helper already has a substantial AutoCombatPanel whole-mechanics follow-up ready for B after repair.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 19:04 EDT

- Active Cloud remains `28 exact / 4 expected-missing / 0 divergent`; approved same-path remains `189/407`.
  Delivery speed, DTO count, and source-only prerequisites do not advance this number.
- A's repaired NPC Ctrl-probe local mechanics is parent source-approved (`P0/P1/P2=0`) after restoring the
  mandatory washed expected-template gate and exact baseline yellow-mask OpenCV cleanup. It remains a prerequisite,
  not a completed Cloud `NpcClickService` chain; A's write set is released for the next substantial cohort.
- B and C now own the disjoint DHXY/Cloud halves of the complete active PlayerState first-aid chain. B's nine Java
  files provide the closed local command/result, strict codec/digest, queue-safe handler and cached-plan mechanics;
  C's ten Java files provide the mirrored Cloud contract/port and all active `AutoBattleTask`/`AutoCombatService`
  callers through `PlayerStateService`. Both are claimed and in progress.
- D's two-Service Summon whole-pass repair is parent source-approved (`P0/P1/P2=0`). The active chain now reaches
  the existing typed DHXY whole-pass exactly once using the authority-minted current context, with all four intent
  fields, nine cleanup fields, five slot states and four transport terminals mapped without new retry/TTL/state
  semantics. It awaits the unified fresh Cloud package after the B/C shared writers stabilize.
- The delivery-preflight helper remains non-binding; the next-task helper must keep a ready queue of substantial,
  mutually exclusive cohorts and explicitly mark missing-context or missing-producer candidates as blocked rather
  than filling worker slots with model/helper-only work. The parent remains the sole reviewer.

No approved business difference; migrate behavior-equivalently from `696a12b0`.

## Phase 3/4 Progress - 2026-07-14 18:24 EDT

- Fast delivery is no longer equated with completion. Phase 1 already preserved all 32 baseline Service bodies, so
  a later cohort may legitimately have a small authored diff; it passes only when the parent proves the complete
  reachable class/caller graph and fresh build gates. Active Cloud remains `28 exact / 4 expected-missing /
  0 divergent`, and approved same-path remains `189/407`.
- D's complete `AutoCombatService` is source-approved after a 27-method whole-class comparison. Its only divergence
  is the two previously approved `CloudUiCleanerPort` substitutions at the original cleanup sites. D immediately
  received the two-Service `TaskMaintenanceService + SummonSkillService` whole-pass cohort, including all gates,
  cooldown/backoff, result mapping, finally state and the real `summonSkillWholePass` terminal.
- C's complete Memory storage chain is source-approved: all three baseline Service bodies are content-equivalent,
  and the only authored configuration change provides exactly one tenant/user-scoped bean for each mutable store.
  The parent corrected the report's literal-byte wording for one LF/CRLF-only file without creating a code blocker.
- A's 349-line Ctrl-probe delivery is parent-blocked (`P1=2`): non-empty expected templates can be bypassed by a
  generic OPTION, raw frames replace the committed washed-template input, and the yellow OCR mask omits the baseline
  OpenCV cleanup/polarity. The original A owns a two-file exact-baseline repair.
- B's six-item Dialog repair is source-approved (`P0/P1/P2=0`), but the cohort remains integration-pending until the
  existing local handler has a real runtime construction/owner path. Source repair approval does not advance the
  count. Preflight and queue helpers continue preparing evidence and substantial READY_NOW follow-up cohorts; the
  parent remains the sole reviewer and runs no concurrent clean build while writers are active.

No approved business difference; migrate behavior-equivalently from `696a12b0`.
