# CR271 TURN-28S2 - NpcClick active Alt-shortcut HTTPS-turn cutover

## PARENT FROZEN CARD - EXTERNAL-B REPLACEMENT READY - 2026-07-16T09:38:31.235-04:00

- Card type: bounded real Cloud production implementation slice of TURN-28; not helper/reviewer work.
- Status: `READY / CLAIM REQUIRED / SOURCE-START OPEN`.
- Owner after true-EOF claim: freshly restarted CR271 External Worker B.
- Business authority: strict `696a12b0ffb8aa21f7d5dee841a65cecd78be9f7`; no approved behavior difference.

## Exact write set

1. Cloud `src/main/java/com/bot/dhxy/service/NpcClickService.java`.
2. This append-only child card.

Initial production: 3374 lines, SHA-256
`cce8f0203ac90a0d39f7cff99dda8d9a616656a55467ed4ae3aa053ad0923441`, byte-identical to the strict-696 mirror.
Every test, recognizer, protocol/context/client, DHXY file, caller, POM/resource and other card is read-only.

## Frozen implementation contract

Replace exactly four active top-level key mechanics in place, preserving their surrounding branch/order/fallback:

1. generic second-pipeline dismount: `ALT_C` then `WAIT 700ms`;
2. confirmed-flying direct-combat dismount: `ALT_C` then `WAIT 700ms`;
3. grounded direct-combat mode entry: `ALT_A` then `WAIT 350ms`;
4. ordinary pipeline name-layer preparation: `ALT_4` then `WAIT 400ms`.

Each reached site must use one real public `TurnGameClient.execute(...)` action with one fresh UUID, exact current
device/window/title/HWND/process/rect correlation, ordered `INPUT KEY_TAP -> WAIT`, and no frame. Only correlated
`COMPLETED/COMPLETED` with both ordered steps may continue to the next strict-696 business line. BUSY/duplicate/
timed-out/interrupted uncertainty, FAILED/STOPPED/DUPLICATE_OR_UNCERTAIN, malformed/correlation/context/metadata
drift are terminal with zero later action; confirmed stop uses the existing checkpoint path, other failures use the
existing fatal path. No auto retry/replay/resend/session/ledger/TTL/durable workflow or local business/OCR truth.

Do not touch legacy private helper sites, mouse/Ctrl/capture/template/OCR/recognizer/dialog/BattleRadar/memory/
navigation/callers. This child has no test write set and cannot satisfy the TURN-28 parent test gate.

## Claim and delivery

External B must append `EXTERNAL-B CLAIMED` here before editing and make a real production increment in its first
5-minute heartbeat window. Delivery is one true-EOF `EXTERNAL-B SOURCE DELIVERED` with final SHA and exact line
evidence, then stop editing. Parent review, later TURN-28 slices/unique named test, two independent reviews and
stable-writer build remain pending. No Maven/JUnit/compile/package/runtime/application/server/Task/UI/capture/input
or Git mutation while writers are active.

<!-- TRUE_EOF: TURN-28S2 PARENT FROZEN EXTERNAL-B-REPLACEMENT-READY CLAIM-REQUIRED ONE-PRODUCTION-FILE STRICT-696 2026-07-16T09:38:31.235-04:00 -->

## PARENT LANE REASSIGNMENT - EXTERNAL-A NEXT - 2026-07-16T10:38:00-04:00

- External B never claimed this child and `NpcClickService.java` remains the exact initial 3374-line SHA
  `cce8f0203ac90a0d39f7cff99dda8d9a616656a55467ed4ae3aa053ad0923441`; therefore no owner or WIP is displaced.
- External A has released TURN-28QP1 and is now the live replacement lane for S2. A must append
  `EXTERNAL-A REPLACEMENT CLAIMED` here before editing, then use the unchanged exact write set and frozen strict-696
  contract above. External B's old assignment is superseded; B must not edit this card if its stale task returns.
- First five-minute window still requires a real production increment, delivery or owner return. This reassignment
  starts source now; it does not wait for TURN-28Q independent review/build.

<!-- TRUE_EOF: TURN-28S2 PARENT-REASSIGNED EXTERNAL-A-NEXT B-NEVER-CLAIMED INITIAL-SHA-UNCHANGED CLAIM-REQUIRED 2026-07-16T10:38:00-04:00 -->

## EXTERNAL-A REPLACEMENT CLAIMED - 2026-07-16T10:41:06.793-04:00

- Lane report: `docs/superpowers/plans/reports/2026-07-16-cr271-external-worker-a.md`. Platform session id
  `76eac05a-e5cd-46a2-a58f-5a07c6573ccc`, model `claude-opus-4-8`. No spawn nickname was surfaced, so none is
  fabricated. Implementation Worker only; not an approval, and I create no reviewer.
- I claim on the **parent's** explicit `PARENT LANE REASSIGNMENT - EXTERNAL-A NEXT` (10:38:00), not on any peer
  suggestion, and only after TURN-28QP1's owner was released by parent Source Review #1 (`0/0/0`).
- Continuing-session disclosure stands: I am the same live A lane, not a freshly restarted task.
- Verified independently before claiming, so no owner or WIP is displaced: Cloud
  `src/main/java/com/bot/dhxy/service/NpcClickService.java` is 3374 lines, SHA
  `cce8f0203ac90a0d39f7cff99dda8d9a616656a55467ed4ae3aa053ad0923441` — exactly this card's initial snapshot, and
  byte-identical to the strict-696 mirror at
  `migration-baseline/696a12b0/src/main/java/com/bot/dhxy/service/NpcClickService.java`. External B left zero
  bytes here. I will not edit B's cards or logs.
- Scope accepted verbatim: replace exactly the four active top-level Alt sites in place, preserving surrounding
  branch/order/fallback — `ALT_C`+`WAIT 700`, `ALT_C`+`WAIT 700`, `ALT_A`+`WAIT 350`, `ALT_4`+`WAIT 400`. Each
  site gets one real public `TurnGameClient.execute(...)` with one fresh UUID, exact current
  device/window/title/HWND/process/rect correlation, ordered `INPUT KEY_TAP -> WAIT`, and no frame. Only
  correlated `COMPLETED/COMPLETED` with both ordered steps continues; BUSY/duplicate/timed-out/interrupted
  uncertainty, `FAILED/STOPPED/DUPLICATE_OR_UNCERTAIN`, malformed/correlation/context/metadata drift are terminal
  with zero later action; confirmed stop uses the existing checkpoint path, other failures the existing fatal
  path.
- I will not: add retry/replay/resend/session/ledger/TTL/durable workflow or local business/OCR truth; touch
  legacy private helper sites, mouse/Ctrl/capture/template/OCR/recognizer/dialog/BattleRadar/memory/navigation or
  callers; widen the write set; add a test (this child has no test write set and cannot satisfy the TURN-28 parent
  test gate).
- Hard constraints: no Maven/JUnit/compile/package/runtime/application/server/Task/UI/capture/input (External C
  owns an active Java repair); zero Git mutation; both repos' dirty/untracked protected; incremental edits against
  current bytes.
- Delivery will be one true-EOF `EXTERNAL-A SOURCE DELIVERED` with final SHA, exact line evidence, baseline and
  the un-run gates named. Never `APPROVED`/`CLOSED`. If context runs out first I will append `OWNER RETURNED`
  with precise WIP/SHA rather than deliver early.

<!-- TRUE_EOF: TURN-28S2 EXTERNAL-A REPLACEMENT CLAIMED SNAPSHOT-VERIFIED-IDENTICAL B-ZERO-BYTES 2026-07-16T10:41:06.793-04:00 -->

## EXTERNAL-A OWNER RETURNED (ZERO BYTES WRITTEN) - 2026-07-16T10:43:15.654-04:00

I am returning the TURN-28S2 implementation owner in my first window, having written **zero** bytes of Java. This
is not a delivery and not an approval. `NpcClickService.java` is untouched at the exact initial 3374-line SHA
`cce8f0203ac90a0d39f7cff99dda8d9a616656a55467ed4ae3aa053ad0923441`, still byte-identical to the strict-696
mirror. No WIP, no partial cutover, nothing for a successor to untangle.

### Why I am returning instead of starting

I claimed at `10:41:06` expecting a bounded one-file edit. Reading the target first changed that estimate, and I
would rather say so now than half-migrate a 3374-line production file:

- `NpcClickService.java` today has **no turn machinery at all**. It contains zero references to
  `TurnGameClient`, `TurnInvocationResult`, `TurnWindowMetadata`, `TurnStep`, `TurnOutcome` or
  `TaskFatalException`. The four target sites (`:625`, `:668`, `:683`, `:949`) are plain
  `inputSequences.submitAndWait(List.of(InputAction.pressAltX(), InputAction.sleep(N)))` calls returning a
  boolean.
- So this card is not four line-swaps. It requires introducing the whole exact-correlation and typed-terminal
  path into this file: latest-metadata read, device/window/title/HWND/process/rect correlation, ordered
  `INPUT KEY_TAP -> WAIT` step construction, one-fresh-UUID execution, both-steps-`COMPLETED/COMPLETED`
  admission, and terminal mapping for BUSY/duplicate/timed-out/interrupted uncertainty,
  `FAILED/STOPPED/DUPLICATE_OR_UNCERTAIN` and malformed/correlation/context/metadata drift — routing confirmed
  stop to the existing checkpoint path and everything else to the existing fatal path. The nearest reference,
  `AutoCombatPanelService.requireTerminalOutcome(...)`, is 66 lines on its own, before `latestMetadata`, the
  step builders and the call sites.
- Every one of those terminal branches is a place where a wrong mapping silently fabricates success or drops a
  stop. I have been caught three times on this card family for exactly that class of error — a relabeled stop, a
  missing final gate, an unfounded barrier — and each time the compile ban meant review was the only backstop. My
  remaining context is not enough to write this machinery **and** re-derive each branch against the bytes to the
  standard the reviewers are rightly applying. Starting it would most likely end as a partial cutover plus an
  `OWNER RETURNED` anyway, but with a damaged file instead of a clean one.

I am not blocked by any gate: TURN-28QP1 is released, the write set is free, and this reassignment explicitly does
not wait for TURN-28Q build. This is purely my own capacity, disclosed rather than discovered later.

### Successor spec (what I verified, so a fresh lane need not re-derive it)

- **Four active sites**, all top-level, each currently `inputSequences.submitAndWait(...)` returning boolean:
  1. `:625` `npcClick:retry:altC-dismount` — `pressAltC()` + `sleep(700)`; on false/stop returns false after a
     warn log; success falls through to `runNpcClickPipeline(request, verifier, "dialog-after-alt-c")`.
  2. `:668` `npcClick:directCombat:altC-dismount` — `pressAltC()` + `sleep(700)`; guarded by
     `flyingState == FLYING`; on false/stop returns
     `DirectCombatClickResult.skipped("direct-combat-dismount-failed-or-stopped")`.
  3. `:683` `npcClick:directCombat:enterAltA` — `pressAltA()` + `sleep(350)`; on false/stop returns
     `DirectCombatClickResult.skipped("direct-combat-alt-a-not-entered")`.
  4. `:949` — `pressAlt4()` + `sleep(400)`, ordinary pipeline name-layer preparation.
  Each site's surrounding branch/order/fallback and log lines must be preserved; only the mechanic changes.
- **Do not touch** `:3271` and `:3302`: these call `inputProvider.pressAlt4()` directly and are the legacy
  private helper sites the card explicitly excludes.
- **Reference pattern to mirror** (same repo, already reviewed):
  `AutoCombatPanelService.java` — `alt8Steps(int waitMs)` `:874-889` builds exactly
  `TurnStep(0, INPUT, KEY_TAP, new TurnInputSpec(null,null,null,null,null,"ALT_8",null), null,null,null,null)`
  followed by `TurnStep(1, WAIT, null, null, (long) waitMs, ...)`; `executeOrdered(...)` calls
  `context.getTurnGameClient().execute(orderedSteps, false, TIMEOUT)` and hands the result plus
  `orderedSteps.stream().map(TurnStep::type).toList()` to `requireTerminalOutcome(...)`; call site `:188-203`
  shows `latestMetadata(context, operation)` first and `TaskFatalException` propagating out.
- **Available in NpcClickService already**: `taskExecutionContextHolder` (`:113`) for the context, and
  `TaskCheckpoint.throwIfStopRequested(taskExecutionContextHolder, "...")` (used at `:333`, `:359`, `:431`) as the
  existing confirmed-stop checkpoint path. `shouldStop()` (`:3253`) is only `Thread.currentThread().isInterrupted()`
  and is **not** a substitute for typed turn terminals.
- The card grants no test write set, so this child cannot satisfy the TURN-28 parent test gate on its own.

### Honest gate record

- Zero Java bytes written; zero Git mutation; both repos' dirty/untracked protected; no other write set, card or
  log touched; no reviewer self-created. I hold no card owner as of this section.
- Nothing was run: no Maven/JUnit/compile/package/runtime/application/server/Task/UI/capture/input. The line
  numbers and SHAs above are reads of current bytes.
- Continuing-session disclosure stands: I am the long-running A lane, not a fresh task, which is precisely why I
  am handing this one back rather than opening it.

<!-- TRUE_EOF: TURN-28S2 EXTERNAL-A OWNER-RETURNED ZERO-BYTES-WRITTEN FILE-AT-INITIAL-SHA SUCCESSOR-SPEC-PROVIDED NOT-COMPILED NOT-RUN 2026-07-16T10:43:15.654-04:00 -->

## PARENT RESTART ASSIGNMENT - FRESH EXTERNAL B NEXT - 2026-07-16T11:03:03.155-04:00

- Parent accepts A's zero-byte owner return. `NpcClickService.java` remains 3374 lines at the frozen strict-696
  SHA `cce8f0203ac90a0d39f7cff99dda8d9a616656a55467ed4ae3aa053ad0923441`; this card has no owner or WIP.
- The old B task never claimed S2 and is stale. **A freshly restarted External B** is now NEXT. It must append
  `EXTERNAL-B RESTART CLAIMED` here before editing and produce a real source increment, delivery or canonical owner
  return in its first five-minute window. An old heartbeat or lane report is not ownership.
- The one-production-file write set and frozen contract above are unchanged: migrate exactly the four active
  top-level `ALT_C+WAIT700`, `ALT_C+WAIT700`, `ALT_A+WAIT350`, `ALT_4+WAIT400` mechanics through one public HTTPS
  turn per reached site, with one fresh UUID, exact correlation, ordered steps and terminal/uncertain zero later
  action. Legacy private sites remain untouched; no test file or wider helper/caller change is authorized.
- No Maven/JUnit/compile/package/runtime/application/server/Task/UI/capture/input or Git mutation was run.

<!-- TRUE_EOF: TURN-28S2 PARENT-RESTART FRESH-EXTERNAL-B-NEXT ZERO-OWNER INITIAL-SHA-UNCHANGED CLAIM-REQUIRED STRICT-696 2026-07-16T11:03:03.155-04:00 -->

## PARENT ASSIGNMENT CORRECTION - CURRENT EXTERNAL B MAY CLAIM WHOLE CARD - 2026-07-16T13:10:00-04:00

- The earlier fresh-task-only restriction is superseded. The current External B implementation lane may claim and
  complete this entire TURN-28S2 card, including all production, card evidence and parent-returned repair.
- The frozen TURN-28S2 contract and write set remain unchanged. Do not split the card or hand off a remainder.
- Until B appends canonical `EXTERNAL-B TURN-28S2 CLAIMED` at true EOF, the card has zero owner.

<!-- TRUE_EOF: TURN-28S2 PARENT-ASSIGNMENT-CORRECTED CURRENT-EXTERNAL-B-MAY-CLAIM-WHOLE-CARD FRESH-ONLY-SUPERSEDED ONE-CARD-ONE-OWNER 2026-07-16T13:10:00-04:00 -->

## PARENT LANE AVAILABILITY RECORD - CURRENT B DECLINED BEFORE CLAIM - 2026-07-16T13:13:00-04:00

- The assignment correction was read successfully, but the current External B task explicitly reported exhausted
  context and declined to claim rather than create partial WIP. It wrote zero card/source bytes and never became
  owner. This is a worker-capacity replacement, not a fresh-task gate in the TURN-28S2 contract.
- TURN-28S2 remains one complete card with zero owner and unchanged frozen source. A replacement External B must
  claim and complete this whole card; it must not split, delegate or return only a remainder.

<!-- TRUE_EOF: TURN-28S2 CURRENT-B-DECLINED-BEFORE-CLAIM CONTEXT-EXHAUSTED ZERO-OWNER ZERO-WIP WHOLE-CARD-REPLACEMENT-REQUIRED 2026-07-16T13:13:00-04:00 -->

## EXTERNAL-B TURN-28S2 CLAIMED - 2026-07-16T13:15:12-04:00

- Implementation Worker：**CR271 External Worker B**(current lane,按父级 `PARENT ASSIGNMENT CORRECTION`(13:10)整卡授权);非 reviewer,不能批准本卡;父级唯一 manager/final reviewer。不含 `APPROVED/CLOSED`,不自批。
- 身份(诚实自报,非平台权威真值):Claude Code 会话 `aa951b1e-8f04-4f92-b6e0-de08af49c39a`(UUIDv4,非平台 spawn 的 `019f…` UUIDv7);自选昵称 `Kepler`。权威身份以平台 spawn 记录为准,父级可追加 `CLAIM IDENTITY CORRECTION`。
- **我承担整卡**:全部 production、卡片证据与后续父级返修,直至父级 source 通过或我明确整卡 `OWNER RETURNED`。**不拆卡、不委派局部、不建子卡、不只交付一部分、不把余量交给他人。**
- 已完整读取:本卡全部内容(冻结合同 1-4、exact write set、claim/delivery)、`PARENT ASSIGNMENT CORRECTION`、A 的 `OWNER RETURNED` successor spec、ACTIVE_WORK 顶部 CR271、两仓 git status/HEAD、以及卡片涉及的实际源码(`NpcClickService` 四站点与周边分支、镜像范式 `AutoCombatPanelService` 的 `alt8Steps`/`executeOrdered`/`requireTerminalOutcome`/`latestMetadata`/`currentContext`)。
- **领取时产线**:`src/main/java/com/bot/dhxy/service/NpcClickService.java` = **3374 行**,SHA-256 `cce8f0203ac90a0d39f7cff99dda8d9a616656a55467ed4ae3aa053ad0923441`(= 卡内 Initial production,亦即经父级实证与 strict-696 mirror 逐字节相同的 S1 交付件)。
- **写集(恰 2 项)**:① 上述 production;② 本 append-only 子卡。其余(全部 test/recognizer/protocol/context/client/DHXY/caller/POM/resource/他卡)只读。
- **基线**:strict `696a12b0ffb8aa21f7d5dee841a65cecd78be9f7`;无已批准业务差异。
- **将实施的四个 active 站点**(其余分支/顺序/fallback/日志逐字保留):①`:625` `npcClick:retry:altC-dismount` ALT_C+WAIT700;②`:668` `npcClick:directCombat:altC-dismount` ALT_C+WAIT700(FLYING 门内);③`:683` `npcClick:directCombat:enterAltA` ALT_A+WAIT350;④`:949` ALT_4+WAIT400(ordinary name-layer)。**明确不动** `:3271`/`:3302`(直接调 `inputProvider.pressAlt4()` 的 legacy 私有点,卡已排除)。
- **每个站点**:一次真实 public `TurnGameClient.execute(...)`、一个新 UUID、exact 当前 device/window/title/HWND/process/rect correlation、有序 `INPUT KEY_TAP -> WAIT`、无 frame;仅 correlated `COMPLETED/COMPLETED` 且两步有序完成才继续下一条 strict-696 业务线。BUSY/duplicate/timed-out/interrupted uncertainty、`FAILED/STOPPED/DUPLICATE_OR_UNCERTAIN`、malformed/correlation/context/metadata drift 一律终止且此后零 action;confirmed stop 走既有 `TaskCheckpoint.throwIfStopRequested`,其余走既有 fatal 路径。**不新增** auto retry/replay/resend/session/ledger/TTL/durable workflow,不引入本地业务或 OCR 真值。
- **禁令**:不碰 legacy 私有 helper 站点、mouse/Ctrl/capture/template/OCR/recognizer/dialog/BattleRadar/memory/navigation/callers;不扩写集;本卡无 test 写集,**不能**满足 TURN-28 父卡 test gate,我不冒充。Java writers 活动期间**不运行** Maven/JUnit/compile/package;不启 runtime/application/server/Task/UI/capture/input;**禁一切 Git mutation**;只从当前字节增量编辑;保护两仓全部 dirty/untracked。基线 DHXY HEAD `0114604e`、cloud-brain `3b988ca`。
- 交付:完成后在本卡 true EOF 追加**一次** canonical `EXTERNAL-B SOURCE DELIVERED` + 最终 SHA + 精确行证据,然后停止编辑;父级独立 source review;后续 TURN-28 切片/唯一 named test/双独立 review/stable-writer build 仍 pending。

<!-- TRUE_EOF: TURN-28S2 EXTERNAL-B TURN-28S2 CLAIMED WHOLE-CARD ONE-PRODUCTION-FILE STRICT-696 Kepler(self-reported; platform id pending) session:aa951b1e-8f04-4f92-b6e0-de08af49c39a 2026-07-16T13:15:12-04:00 -->

## EXTERNAL-B SOURCE DELIVERED - 2026-07-16T13:18:31-04:00

整卡 TURN-28S2 按冻结合同完成。**本段不是 approval**;不含 `APPROVED/CLOSED`;不自批;请父级独立 source review。我保留整卡 ownership 直至父级通过或明确整卡 OWNER RETURNED,并承担后续返修。

**写集(恰 2 项,无扩张)**:① Cloud `src/main/java/com/bot/dhxy/service/NpcClickService.java`;② 本卡。其余零改动:未碰任何 test/recognizer/protocol/context/client/DHXY/caller/POM/resource/他卡。

| 文件 | 领取时 | 交付时 |
|---|---|---|
| `NpcClickService.java` | 3374 行 / `cce8f0203ac90a0d39f7cff99dda8d9a616656a55467ed4ae3aa053ad0923441` | **3510 行** / **`13f0ea0d4e96829be1cc5e3f7042ea5c7ad30db56c6293466438f6a7a7c1053c`** |

**四个 active 站点已逐一改走 public HTTPS turn(周边分支/顺序/fallback/日志逐字保留)**:
1. `:638` generic second-pipeline dismount —— `executeAltShortcutTurn("ALT_C", 700, "npcClick:retry:altC-dismount")`;`if (!dismountSubmitted || shouldStop())` 的 warn 日志与 `return false` 原样;成功仍落 `runNpcClickPipeline(request, verifier, "dialog-after-alt-c")`。
2. `:678` confirmed-flying direct-combat dismount —— `executeAltShortcutTurn("ALT_C", 700, "npcClick:directCombat:altC-dismount")`;仍在 `flyingState == FLYING` 门内,其后 info 日志与 `DirectCombatClickResult.skipped("direct-combat-dismount-failed-or-stopped")` 原样;`UNKNOWN` 跳过分支未动。
3. `:691` grounded direct-combat mode entry —— `executeAltShortcutTurn("ALT_A", 350, "npcClick:directCombat:enterAltA")`;warn 日志与 `skipped("direct-combat-alt-a-not-entered")` 原样。
4. `:954` ordinary pipeline name-layer preparation —— `executeAltShortcutTurn("ALT_4", NPC_PIPELINE_HIDE_PLAYER_NAMES_SETTLE_MS, "npcClick:pipeline-hide-player-names:" + request.npcName())`;**沿用既有常量(=696 的 400ms)不硬编码**;前置 `shouldStop()` 早返、info 日志与 `return ok && !shouldStop()` 原样。

**新增私有 mechanics(仅本文件,无新 production 文件/无顶层 model)**:
- `executeAltShortcutTurn(String key, int waitMs, String operation)`(`:3278`):`taskExecutionContextHolder.current()` 取 exact context → `TaskCheckpoint.throwIfStopRequested` → `latestExactWindowMetadata(...)` → 有序两步 `TurnStep(0, INPUT, KEY_TAP, new TurnInputSpec(null,null,null,null,null,key,null), null,null,null,null)` + `TurnStep(1, WAIT, null, null, (long) waitMs, null,null,null)` → **一次** `context.getTurnGameClient().execute(orderedSteps, false, NPC_CLICK_TURN_TIMEOUT)`(**一个新 UUID、一条 command、零 retry/replay/resend**)→ 终态投影;仅 `COMPLETED` 返 true。
- `requireAltShortcutTerminalOutcome(...)`:`commandStatus != COMPLETED` → `INTERRUPTED_UNCERTAIN` 先 `TaskCheckpoint.throwIfStopRequested`,其余(含 BUSY/DUPLICATE_ACTION_ID/TIMED_OUT_UNCERTAIN)走 fatal;`STOPPED` → checkpoint,未确认停止则 fatal;`DUPLICATE_OR_UNCERTAIN` → fatal;非 COMPLETED/FAILED → fatal;`!expectedWindow.equals(outcome.window())` → fatal(exact device/window/title/HWND/process/rect correlation);step 数/index/type/status/match/localResultJson 任一不符 → fatal;`invocation.frame() != null` → fatal(**input action 不得返回 frame**)。**全部终态皆零后续 action**。
- `latestExactWindowMetadata(...)`:`latestWindowMetadata()` 缺失 → fatal;`deviceId`/`windowId` 与 bound turn context 不符 → fatal;rect 空或非正 → fatal。
- `npcClickFatal(operation, detail)` → `TaskFatalException("npc click " + operation + " failed: " + detail)`(既有 fatal 路径)。
- 常量 `NPC_CLICK_TURN_TIMEOUT = Duration.ofMillis(120_000L)`(`:148`)。
- 新增 import:`TurnInputAction`/`TurnInputSpec`/`TurnOutcome`/`TurnStep`/`TurnStepResult`/`TurnStepType`/`TurnWindowMetadata`/`TurnWindowRect`/`TaskExecutionContext`/`TaskFatalException`/`CloudTurnCommandResult`/`TurnInvocationResult`/`java.time.Duration`(路径均取自同仓已审范式 `AutoCombatPanelService`)。

**保留验证(grep 实证)**:
- **legacy 私有 helper 站点未动**:`inputProvider.pressAlt4()` 仍在 `:3407`/`:3438`(卡明确排除)。
- **写集外站点未动**:`inputSequences.*` 仍 **5** 处、`InputAction.*` 仍 **13** 处 → 二者 import/字段均**仍被使用,故保留**(非死码)。
- 未碰 mouse/Ctrl/capture/template/OCR/recognizer/dialog/BattleRadar/memory/navigation/callers;**未新增** auto retry/replay/resend/session/ledger/TTL/durable workflow,无本地业务或 OCR 真值;机械 key tap 本身不构成业务成功,仅解锁调用点既有的 696 基线分支。

**未运行门**:**未运行** Maven/JUnit/compile/package(Java writers 活动中);未启 runtime/application/server/Task/UI/capture/input;**未执行任何 Git mutation**;两仓全部 dirty/untracked 与他人半成品完好;DHXY HEAD `0114604e`、cloud-brain `3b988ca` 未动。本卡无 test 写集,**不能**满足 TURN-28 父卡 test gate,我不冒充;后续 TURN-28 切片/唯一 named test/双独立 review/stable-writer build 仍 pending。自此停止编辑,等父级 review 或返修(返修我回本卡就地做)。

<!-- TRUE_EOF: TURN-28S2 EXTERNAL-B SOURCE DELIVERED one-production-file 3374->3510 sha 13f0ea0d4e96829be1cc5e3f7042ea5c7ad30db56c6293466438f6a7a7c1053c four-alt-sites-turn-cutover strict-696 Kepler(self-reported; platform id pending) session:aa951b1e-8f04-4f92-b6e0-de08af49c39a 2026-07-16T13:18:31-04:00 -->

## PARENT WHOLE-CARD SOURCE REVIEW #1 - REPAIR REQUIRED - 2026-07-16T13:24:24-04:00

- Verdict: `P0/P1/P2=0/1/0 / REPAIR #1 REQUIRED`.
- Accepted and frozen for this repair: the four intended active call sites, one public HTTPS turn per reached site,
  ordered `INPUT KEY_TAP -> WAIT`, fresh command UUID, exact-window equality, no frame, and fatal handling for
  command uncertainty, `STOPPED` without confirmed stop, `DUPLICATE_OR_UNCERTAIN`, correlation drift and malformed
  step/frame evidence. The two excluded legacy private `inputProvider.pressAlt4()` sites remain untouched.
- **P1 - a correlated `FAILED` outcome is incorrectly projected back into the legacy boolean fallback.** The frozen
  card says `FAILED` is terminal and, unless it is a confirmed stop, must leave through the existing fatal path.
  Instead `requireAltShortcutTerminalOutcome(...)` accepts `FAILED` and returns it (`NpcClickService.java:3339-3372`),
  while `executeAltShortcutTurn(...)` converts it to `false` (`:3304`). The four migrated callers then execute their
  old false branches (`:639-644`, `:682-695`, `:954-960`) rather than throwing `TaskFatalException`. This changes the
  frozen terminal contract and can present a failed remote input as an ordinary skipped/fallback business result.
- Required whole-card repair: keep the current validation of the legal FAILED shape, but after validation route every
  correlated `FAILED` outcome through `npcClickFatal(...)`; only correlated `COMPLETED` with both steps completed may
  return success. Update the method JavaDoc so it no longer promises `false` for FAILED. Do not change surrounding
  strict-696 branch order/fallbacks, add retries, widen the write set, or split this repair.
- The entire card is returned to the same External B implementation Worker. B remains responsible for production,
  this fixed report and any subsequent whole-card repair until parent source review passes or B returns the whole card.
- Review was source-only while A/C writers are active. No Maven/JUnit/compile/package/runtime/application/server/
  Task/UI/capture/input or Git mutation was run.

<!-- TRUE_EOF: TURN-28S2 PARENT WHOLE-CARD SOURCE REVIEW-1 REPAIR-1-REQUIRED P0-0-P1-1-P2-0 FAILED-MUST-FATAL RETURNED-TO-EXTERNAL-B 2026-07-16T13:24:24-04:00 -->

## EXTERNAL-B REPAIR #1 SOURCE DELIVERED - 2026-07-16T13:30:07-04:00

回应 `PARENT WHOLE-CARD SOURCE REVIEW #1`(13:24:24,`P0/P1/P2=0/1/0`)。**P1 完全成立,我接受,无异议。** 本段不是 approval;不含 `APPROVED/CLOSED`;不自批。整卡 ownership 仍在我,继续承担后续返修。

**根因(我的错误)**:我镜像同仓已审范式 `AutoCombatPanelService` 时,把它的 `allowKnownFailure=true` 语义一并带了过来 —— 那是**该服务**的合同,不是本卡的。S2 冻结合同写明 `FAILED` 是 terminal、除确认停止外须走既有 fatal;我却让 correlated `FAILED` 经 `requireAltShortcutTerminalOutcome(...)` 返回、再由 `executeAltShortcutTurn(...)` 折成 `false`,使四个迁移调用点走各自的 legacy false 分支 —— **等于把一次失败的远程输入伪装成普通的 skipped/fallback 业务结果**,正是本卡与 696 都禁止的"机械结果冒充业务真值"。

| 文件 | Review #1 时 | 返修后 |
|---|---|---|
| `NpcClickService.java` | 3510 行 / `13f0ea0d4e96829be1cc5e3f7042ea5c7ad30db56c6293466438f6a7a7c1053c` | **3527 行** / **`aa50ae7cb6fd9fe5c494225090ec123742d67c1faea9d154e7e01bafb1a72862`** |

**返修内容(严格按 Required whole-card repair,写集仍恰 2 项)**:
1. **保留 FAILED 的合法形状校验**:step 数/index/type、前缀 `COMPLETED`、`failedStepIndex` 处 `FAILED`、其后 `NOT_RUN`、`match`/`localResultJson` 为空、metadata 相等、无 frame —— **全部原样在前**,以便 malformed failure 仍被报为 malformed 而非普通失败。
2. **校验通过后,correlated `FAILED` 一律走既有 fatal**:新增 `if (outcome.status() == TurnOutcome.Status.FAILED) throw npcClickFatal(operation, "turn outcome failed at step=" + outcome.failedStepIndex() + ", code=" + outcome.code());`,并附注释说明为何不得折回 boolean。
3. **`executeAltShortcutTurn(...)` 不再有 false 返回**:去掉 `return outcome.status() == COMPLETED`,改为校验后 `return true`;仅 correlated `COMPLETED` 且两步完成才可能返回。
4. **JavaDoc 已更新,不再承诺 FAILED → false**:改为明确"always true;never returns false",并列明 FAILED/命令不确定/未确认 STOPPED/DUPLICATE_OR_UNCERTAIN/correlation drift/malformed step-frame 全部 terminal,经 checkpoint 或 fatal 离开;`requireAltShortcutTerminalOutcome` 的 JavaDoc 同步更正。

**未改动验证(grep 实证)**:
- **四站点周边 strict-696 分支顺序/fallback/日志逐字未动**:`:640` retry 的 warn + `return false`、`:683` `skipped("direct-combat-dismount-failed-or-stopped")`、`:695` `skipped("direct-combat-alt-a-not-entered")`、`:960` `return ok && !shouldStop()` 全部保留 —— 它们现在只由既有 `shouldStop()` 等 696 语义驱动,不再可能被远程失败冒充。
- **未加 retry**、未扩写集、未拆分本返修;`FAILED` 形状校验相关符号仍 5 处;legacy 私有 `inputProvider.pressAlt4()` 仍 2 处(`:3407`/`:3438`)未动;四站点仍各为一次 public turn、一新 UUID、一 command、有序 `INPUT KEY_TAP -> WAIT`、无 frame、exact-window 相等。
- 未新增 session/ledger/TTL/durable workflow;无本地业务或 OCR 真值。

**未运行门**:Java writers(A/C)活动中,**未运行** Maven/JUnit/compile/package;未启 runtime/application/server/Task/UI/capture/input;**未执行任何 Git mutation**;两仓 dirty/untracked 与他人半成品完好;DHXY HEAD `0114604e`、cloud-brain `3b988ca` 未动。本卡无 test 写集,不能满足 TURN-28 父卡 test gate,我不冒充。自此停止编辑,等父级复审;若再返修我仍回本卡就地做。

<!-- TRUE_EOF: TURN-28S2 EXTERNAL-B REPAIR-1 SOURCE DELIVERED failed-now-fatal 3510->3527 sha aa50ae7cb6fd9fe5c494225090ec123742d67c1faea9d154e7e01bafb1a72862 Kepler(self-reported; platform id pending) session:aa951b1e-8f04-4f92-b6e0-de08af49c39a 2026-07-16T13:30:07-04:00 -->

## PARENT WHOLE-CARD SOURCE REVIEW #2 - PASSED - 2026-07-16T13:34:08-04:00

- Verdict: `P0/P1/P2=0/0/0 / SOURCE REVIEW PASSED`. External B implementation owner is released.
- The legal correlated FAILED shape is still fully validated first. After metadata, step sequence/status and no-frame
  validation, every `FAILED` now throws `npcClickFatal(...)`; `executeAltShortcutTurn(...)` returns only after a
  correlated `COMPLETED` outcome with both ordered steps completed. JavaDoc now matches that closed contract.
- The four strict-696 call sites, one-command/fresh-UUID mechanics, exact-window correlation and all previously
  accepted uncertainty/STOP/drift defenses remain unchanged. No retry, replay, session, ledger, TTL or business
  fallback was added.
- Frozen production SHA: `aa50ae7cb6fd9fe5c494225090ec123742d67c1faea9d154e7e01bafb1a72862`
  (3527 lines). This card has no test write set and does not by itself satisfy the TURN-28 parent test gate.
- No Maven/JUnit/compile/package/runtime/application/server/Task/UI/capture/input or Git mutation was run while
  C/D Java writers are active. Two independent whole-card reviewers are next; this is not CARD APPROVED.

<!-- TRUE_EOF: TURN-28S2 PARENT-REVIEW-2 PASSED P0P1P2=0/0/0 SOURCE-REVIEW-PASSED OWNER-RELEASED DUAL-INDEPENDENT-REVIEW-BUILD-PENDING 2026-07-16T13:34:08-04:00 -->

## PARENT REVIEWER-SCOPE ADJUDICATION #3 - R2 BLOCKER REJECTED FOR THIS CARD - 2026-07-16T13:40:00-04:00

- R1 verdict: `APPROVED / P0/P1/P2=0/0/0`.
- R2 reported a real cross-card risk: Cloud `WubeiTask.runRoundPhases()` catches generic `RuntimeException`, so a
  `TaskFatalException` from this Service can enter Wubei round recovery. Parent independently reproduced that fact.
- The finding does not reopen or expand TURN-28S2. This frozen card explicitly makes every caller read-only and owns
  only `NpcClickService.java`; its stated boundary is to route non-confirmed-stop terminal outcomes through the
  existing fatal path, which the delivered Service now does. The authoritative DAG already has the complete
  `TURN-35: WubeiTask complete turn wiring` card, whose exclusive production write set is `task/wubei/WubeiTask.java`
  and whose acceptance includes whole-task retry/fallback/terminal behavior. Moving that caller edit into S2 would
  violate both exact write sets and the user's one-existing-whole-card ownership rule.
- The Wubei generic-catch risk is therefore recorded as a mandatory TURN-35 whole-card acceptance point, not an S2
  P1. R2 must re-review the same S2 SHA against S2's frozen Service boundary and issue a latest-round verdict; parent
  does not convert the reviewer verdict by assertion.
- No Java or card write set was expanded. No Maven/runtime/input or Git mutation was run.

<!-- TRUE_EOF: TURN-28S2 PARENT-ADJUDICATION-3 R2-CROSS-CARD-FINDING-REAL BUT-S2-BLOCKER-REJECTED CALLER-READONLY TURN-35-OWNS-WUBEI-FATAL-RETHROW R2-REREVIEW-REQUIRED 2026-07-16T13:40:00-04:00 -->

## PARENT LATEST WHOLE-CARD REVIEW/BUILD STATE - 2026-07-16T14:40:21-04:00

- Parent source Review #2 and both latest independent whole-card reviewer rounds are
  `APPROVED P0/P1/P2=0/0/0`; the independent gate is `PASSED 2/2`.
- Stable-writer Cloud build gate #1 is blocked before tests by shared main compilation. The failures are in
  incomplete whole-card migration owners including `WubeiTask`, `NavigationService`, `NpcClickService`,
  `DialogService`, and `PlayerStateService`, which still import DHXY-only classes absent from Cloud.
- TURN-28S2 has no test write set and is not returned for source repair. It remains
  `DUAL REVIEW PASSED 2/2 / CLOUD BUILD BLOCKED BY PLANNED WHOLE-CARD PREREQUISITES / NOT CARD APPROVED`.
  The already-recorded Wubei fatal-rethrow acceptance remains owned by complete TURN-35.
- No runtime/application/server/Task/UI/capture/input or Git mutation was run.

<!-- TRUE_EOF: TURN-28S2 PARENT-LATEST-WHOLE-CARD-STATE SOURCE-REVIEW-PASSED DUAL-REVIEW-PASSED-2-OF-2 CLOUD-MAIN-COMPILE-BLOCKED-BY-PLANNED-WHOLE-CARD-PREREQUISITES NO-S2-SOURCE-REPAIR NOT-CARD-APPROVED 2026-07-16T14:40:21-04:00 -->
