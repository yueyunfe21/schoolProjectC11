# G004 野外战斗

## 状态

- `SOURCE+TEST REVIEW PASSED / P0-P1-P2=0-0-0 / FRESH RUNTIME REQUIRED`
- Client：`D:\mavenProject\DHXY-cr271` / `thin-client-design`
- Cloud：`D:\mavenProject\dhxy-cloud-brain` / `navigation-migration`
- 用户业务基线 `D:\mavenProject\DHXY` 只读，不写入。

## 用户确认合同

1. 新任务代码为 `G004 / WILD_BATTLE`，显示名为“野外战斗”。
2. 任务允许本地组队，也允许单人：
   - 同队队员沿用现有角色预检，分配为 `AUTO_BATTLE`；
   - 队长执行 `WILD_BATTLE`；
   - 没有队员的单窗口按队长执行。
3. 操作窗口循环右键点击四个窗口相对坐标：
   - `(284,224)`、`(174,409)`、`(809,623)`、`(813,194)`；
   - 每次 X/Y 分别随机 `[-15,+15]`；
   - 每次右键后等待 `1000ms`，再处理下一个点。
4. 每次“移动鼠标 + 右键 + 等待 1000ms”必须是一个不可拆分的本地输入包。
5. 多个操作组复用既有 Cloud FIFO `CloudTaskTurnCoordination`：
   - 当前组在战前点击循环中持续持有 task turn；
   - 只有 Client Runner 报告 `IN_COMBAT` 后才释放；
   - 战斗中不点击；
   - Runner 报告 `COMBAT_EXITED` 后重新排队获取 turn 并继续循环。
6. Client Runner 是战斗状态唯一裁决者。Cloud 不新增 Battle Radar、截图识别或二次确认。
7. 运行时长使用 UI“分钟”：
   - `0` 表示无限；
   - 到达总时长后立即结束任务，即使当前仍在战斗，也不等待脱战。
8. 停止、异常、期限结束均必须 `forceRelease` 当前 task turn，不得遗留占用。

## 写集

### Client

- `TaskType`、`TurnTaskCode`
- `BotProperties`
- `WindowTaskControlService`
- `MainWindowController`
- 对应协议/映射测试

### Cloud

- `TaskType`、`TurnTaskCode`
- `CloudTurnTaskFactory`
- `CloudTurnTaskRuntime`
- 新增 `WildBattleTask`
- 既有 `CloudTaskTurnCoordination` 增加同一 FIFO authority 内的有界等待；
  超时只原子撤销当前 exact waiter，不新增锁、队列或调度器
- 对应任务合同测试

## 验收

- 双仓 `TurnTaskCode` byte-identical。
- 四个基础点、随机边界、右键与 1000ms 等待顺序有确定性测试。
- `IN_COMBAT` 释放 turn，战斗期间零输入；`COMBAT_EXITED` 后恢复排队。
- 有限时长在战斗中到期也立即结束；停止/失败不泄漏 turn。
- Client/Cloud 聚焦 named tests 与 compile 均通过。
- 禁止启动 runtime、UI、截图或物理输入。

## 交付与父级终审

- Client：
  - 新增 `WILD_BATTLE` 协议/任务类型、分钟配置、UI 任务入口与 `0=无限` 显示；
  - 队列提交继续使用现有 exact-window turn executor；
  - `WindowRemoteTurnControlContractTest#everySupportedLocalTaskTypeMapsToItsExactWireCode`
    通过，main compile 与 test-compile 均 exit `0`。
- Cloud：
  - `WildBattleTask` 按四个相对点循环；每个点生成独立 `±15px` 偏移，并把
    `MOVE_MOUSE + CLICK_RIGHT + WAIT 1000ms` 放在同一命令包；
  - 战前持续持有现有 FIFO task turn；Runner 更新 exact `GameContext` 为
    `IN_COMBAT` 后释放并等待 `COMBAT_STATE_CHANGED`；脱战后重新 FIFO 排队；
  - 有限时长等待 turn 时使用 authority 的有界 FIFO admission，期限到达会原子撤销
    exact waiter；战斗等待最多本地 park `1000ms` 后复核 stop/deadline，不产生 Cloud
    视觉请求；
  - 未引用 `BattleRadarService`、Cloud 视觉判断、导航、OCR 或维护逻辑。
- 聚焦验证：
  - `WildBattleTaskContractTest` 3/3；
  - `CloudWholeTaskFoundationContractTest#
    timedEnterCancelsItsExactFifoWaiterWithoutBlockingTheNextOwner` 1/1；
  - Cloud main compile exit `0`；
  - 双仓 `TurnTaskCode` byte-identical；`TurnProtocolValidator` 归一化换行后无内容差异。
- 已知门禁：
  - Cloud 全仓 Maven `testCompile` 仍被本卡之前已存在的旧测试源码/API 漂移阻断，
    包括已删除的 `CloudWholeTaskObserver.probeTypedPathing/classify`、旧构造器与
    `CloudNavigationPathingState.updateObserved`；本卡测试已独立编译并真实执行通过。
  - 未启动 runtime、UI、截图或物理输入。Fresh 验收需重启双端后验证：单窗口循环、
    多操作组 FIFO、进战放权、脱战恢复、有限时长终止。

## 2026-07-28 预留紫字水平巡逻点算法（未接线）

- 用户明确本轮只新增算法，不接入 `G004 / WILD_BATTLE` 或 `AUTO_BATTLE` 运行流程。
- Cloud `SmartClickRecognizer.findHorizontalPatrolPoints(raw, playerName)`：
  - 输入必须是调用方执行 `Alt+4` 后取得的 fresh 窗口图；
  - 复用现有紫字清洗、OCR、候选框和角色名定位；
  - 仅此新 API 允许最短一个角色名字符的宽松命中，既有 NPC 紫字公式仍保持至少两字；
  - 返回名字中心、`(x-100,y)` 和 `(x+100,y)`，坐标均为输入图 image-local；完整窗口图即窗口相对坐标；
  - 不发送输入、不截图、不重试、不循环点击、不保存全局状态。
- 未来调用任务负责：`Alt+4`、fresh capture、调用一次并缓存两个点；进入和退出战斗后继续复用缓存，
  不重复识别角色名。
- 本轮 Cloud `mvn -q -DskipTests=false compile` exit `0`；未运行 runtime、UI、capture 或物理输入。

## 2026-07-31 运行返修：战后放大镜单帧漏检不能永久卡住

- 新鲜运行 `remote-turn-7f452350...` 的 `hwnd-3AB11B6` 在前三次战斗后都曾恢复巡逻；第四次
  `AutoCombat` 后，Cloud 后续巡逻请求持续收到 Client `LOCAL_COMBAT_ACTIVE`，直至人工停止。
- 当前 Client Runner 仅在 `minimap_visible_anchor.png` 命中时发布 `COMBAT_EXITED`。该模板为
  `20x22`、ROI `(196,65,20,22)`、阈值 `0.85`；本轮没有改动模板、ROI 或阈值。日志没有保存当时
  原始 ROI 或相似度分数，因此不能把单次漏检武断归因于模板内容；但单个微小锚点在重绘/采样帧上短暂
  漏检是必须容错的输入。
- 已按本地 Runner 唯一战斗权威修复为互证状态机：
  - 放大镜可见：立即 `COMBAT_EXITED`；
  - 放大镜明确不可见且所有本地战斗模板也明确不可见：立即 `COMBAT_EXITED`；
  - 任一采样源不可用：保持 `UNKNOWN`，不得误放开输入。
- 本修复只改 Client `WindowObservationSampler`，不让 Cloud 重新判断战斗。Client main compile 已通过；
  聚焦测试受既有 `WindowTurnLoopContractTest` 的测试源码/API 漂移阻断，需先修复该独立 test-compile
  问题后再重跑 named test。Fresh runtime 验收：至少连续四次进出战斗，战斗中零巡逻输入、每次脱战后
  自动恢复巡逻。

## 2026-07-31 巡逻四点几何返修

- 用户实测野外战斗四点跑成斜四边形。根因是 Cloud 基础点只做过 X 轴局部调整，
  `(334,224) -> (274,409) -> (759,623) -> (763,194)` 本身不满足矩形几何；每点 `+/-15px`
  随机不是根因。
- 已收敛为窗口相对、顺时针正方形：`(334,194) -> (334,623) -> (763,623) -> (763,194)`。
  横纵边长均为 `429px`；合同测试锁定四角、轴对齐与等边约束。
- Fresh runtime 验收：右键四点仍按原顺序循环，整体为正方形，仅允许每角 `+/-15px` 随机微扰。
- Cloud `mvn -q compile` 已通过。指定合同测试被既有全仓 test-compile 错误阻断：
  `CloudTurnTaskFactoryAllowlistTest` 构造器参数漂移、`YipinGuardClickPlanTest` 非 boolean 断言；
  与本次几何常量无关，未在本卡扩大修复范围。

### 2026-07-31 补充：右上点安全边界

- 用户确认左右 X 不能再向外扩，改动只向内对齐为左 `x=334`、右 `x=759`。
- 旧右上 `y=194` 过高，偶尔点击到界面。当前四点为：
  `(334,224) -> (334,623) -> (759,623) -> (759,224)`；保持原顺序和每点 `+/-15px` 微扰。

### 2026-07-31 最终几何：闭环正方形

- 用户指出上版仍为 `425x399` 矩形。保持已确认的左右 X 与安全上边，将底边改为 `y=649`：
  `(334,224) -> (334,649) -> (759,649) -> (759,224)`，四边均为 `425px`。
- 在 `1024x768` 游戏窗口内计入 `+/-15px` 随机，活动范围为 `x=319..774`、`y=209..664`；
  既不会向左右界面扩张，也不会重新落入旧右上界面区。
