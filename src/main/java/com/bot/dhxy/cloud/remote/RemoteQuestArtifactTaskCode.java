package com.bot.dhxy.cloud.remote;

/**
 * Closed wire token set for quest-detail artifact intents.
 *
 * <p>The enum name is the exact canonical wire token; the only current member is {@code XIULUO}.
 * Tokens must stay letter-for-letter identical to the Cloud enum
 * {@code com.yueyunfe.dhxy.cloudbrain.remote.QuestArtifactTaskCode}.</p>
 */
public enum RemoteQuestArtifactTaskCode {
    XIULUO
}
