package com.bot.dhxy.cloud.turn.protocol.observation;

/** Identity transition represented by an observation pathing fact. */
public enum ObservationPathingTransition {
    CURRENT,
    CLEARED,
    REPLACED
}
